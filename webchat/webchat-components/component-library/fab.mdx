## Fab (Floating Action Button)

The Fab component is pretty straightforward, it just a simple button that is used to toggle the webchat interface

### Props

#### onClick

Takes a MouseEventHandler

### Usage

```ts
import { Fab } from '@botpress/webchat'
<Fab onClick={() => setToggleWebchat(!toggleWebchat)}></Fab>
```
