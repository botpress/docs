{/* This file is auto-generated. Do not edit directly. */}
{/* vale off */}

Here's a reference for all [Cards](/learn/reference/cards/) available with the integration:

### Track Event

Track event in Google Analytics

**Input**:

<Expandable title="input fields">
  <ResponseField
    name="userId"
    type="string"
    required
  >
    The user ID to track
  </ResponseField>
  <ResponseField
    name="eventName"
    type="string"
    required
  >
    The event name to track
  </ResponseField>
  <ResponseField
    name="eventPayload"
    type="string"
  >
    The properties of the event as a JSON String
  </ResponseField>
</Expandable>

**Output**:

This Card has no output.

### Track Node

Tracks when a node has been triggered, recording it as a page view in Google Analytics.

**Input**:

<Expandable title="input fields">
  <ResponseField
    name="userId"
    type="string"
    required
  >
    The user ID to track
  </ResponseField>
  <ResponseField
    name="nodeId"
    type="string"
    required
  >
    A unique ID representing the Node being tracked
  </ResponseField>
</Expandable>

**Output**:

This Card has no output.

### Update User Profile

Updates the User's profile in Google Analytics

**Input**:

<Expandable title="input fields">
  <ResponseField
    name="userId"
    type="string"
    required
  >
    The user ID of the profile you want to update
  </ResponseField>
  <ResponseField
    name="userProfile"
    type="string"
  >
    JSON String of a user's metadata (e.g., email, name)
  </ResponseField>
</Expandable>

**Output**:

This Card has no output.


{/* vale on */}