{/* This file is auto-generated. Do not edit directly. */}
{/* vale off */}

Here's a reference for all [Cards](/learn/reference/cards/) available with the integration:

### Get Products

Get products from the Magento instance

**Input**:

<Expandable title="input fields">
  <ResponseField
    name="searchCriteria"
    type="string"
  >
    The search criteria to use for the request
  </ResponseField>
</Expandable>

**Output**:

<Expandable title="output">
  <ResponseField
    name="result"
    type="object"
    required
  >
    

    <Expandable>
  <ResponseField
    name="items"
    type="array"
    required
  >
    
  </ResponseField>
  <ResponseField
    name="search_criteria"
    type="object"
    required
  >
    
  </ResponseField>
  <ResponseField
    name="total_count"
    type="number"
    required
  >
    
  </ResponseField>
    </Expandable>
  </ResponseField>
</Expandable>

### Get Stock Item

Get stock information for a product by SKU

**Input**:

<Expandable title="input fields">
  <ResponseField
    name="sku"
    type="string"
    required
  >
    The SKU of the product to get stock information for
  </ResponseField>
</Expandable>

**Output**:

<Expandable title="output">
  <ResponseField
    name="qty"
    type="number"
  >
    The quantity in stock
  </ResponseField>
  <ResponseField
    name="is_in_stock"
    type="boolean"
  >
    Whether the item is in stock
  </ResponseField>
  <ResponseField
    name="error"
    type="string"
  >
    Error message if request failed
  </ResponseField>
</Expandable>

### Sync Products to Botpress Table

Sync products from Magento to a Botpress table. Creates the table automatically if it doesn't exist with standard product columns.

**Input**:

<Expandable title="input fields">
  <ResponseField
    name="table_name"
    type="string"
    required
  >
    Name of the Botpress table to sync products to (will be created automatically if it doesn't exist)
  </ResponseField>
  <ResponseField
    name="custom_attributes"
    type="string"
  >
    Comma-separated list of custom product attributes to sync (e.g., "color,tent_outer_material,tent_type")
  </ResponseField>
  <ResponseField
    name="filters_json"
    type="string"
  >
    JSON array of filter objects, e.g. [{"field": "price", "condition": "gt", "value": "100"}]
  </ResponseField>
  <ResponseField
    name="_currentPage"
    type="number"
  >
    Current page number
  </ResponseField>
  <ResponseField
    name="_totalCount"
    type="number"
  >
    Total number of products
  </ResponseField>
  <ResponseField
    name="_tableId"
    type="string"
  >
    ID of the Botpress table
  </ResponseField>
  <ResponseField
    name="_runId"
    type="string"
  >
    Sync run ID
  </ResponseField>
  <ResponseField
    name="_customAttributeCodes"
    type="array"
  >
    Custom attribute codes
  </ResponseField>
  <ResponseField
    name="_attributeMappings"
    type="string"
  >
    Attribute mappings as a JSON string
  </ResponseField>
  <ResponseField
    name="_filterCriteria"
    type="string"
  >
    Filter criteria string
  </ResponseField>
  <ResponseField
    name="_currentPageProductIndex"
    type="number"
  >
    Current product index within the page
  </ResponseField>
</Expandable>

**Output**:

<Expandable title="output">
  <ResponseField
    name="success"
    type="boolean"
    required
  >
    Whether the sync was successful
  </ResponseField>
  <ResponseField
    name="synced_count"
    type="number"
    required
  >
    Number of products synced
  </ResponseField>
  <ResponseField
    name="total_count"
    type="number"
    required
  >
    Total number of products found
  </ResponseField>
  <ResponseField
    name="table_name"
    type="string"
    required
  >
    Name of the table products were synced to
  </ResponseField>
  <ResponseField
    name="status"
    type="string"
    required
  >
    Status of the sync
  </ResponseField>
  <ResponseField
    name="error"
    type="string"
  >
    Error message if sync failed
  </ResponseField>
</Expandable>


{/* vale on */}