{/* This file is auto-generated. Do not edit directly. */}
{/* vale off */}

Here's a reference for all [Cards](/learn/reference/cards/) available with the integration:

### Create File

Create an empty file in Google Drive

**Input**:

<Expandable title="input fields">
  <ResponseField
    name="name"
    type="string"
    required
  >
    The name of the file
  </ResponseField>
  <ResponseField
    name="parentId"
    type="string"
  >
    The ID of the file that is the parent of this file. If not set, 'My Drive' is the parent
  </ResponseField>
  <ResponseField
    name="mimeType"
    type="string"
    required
  >
    The media type of the file
  </ResponseField>
</Expandable>

**Output**:

<Expandable title="output">
  <ResponseField
    name="id"
    type="string"
    required
  >
    The ID of the Google Drive file
  </ResponseField>
  <ResponseField
    name="name"
    type="string"
    required
  >
    The name of the file
  </ResponseField>
  <ResponseField
    name="parentId"
    type="string"
  >
    The ID of the file that is the parent of this file. If not set, 'My Drive' is the parent
  </ResponseField>
  <ResponseField
    name="mimeType"
    type="string"
    required
  >
    The media type of the file
  </ResponseField>
  <ResponseField
    name="size"
    type="number"
    required
  >
    The size in bytes of the file
  </ResponseField>
  <ResponseField
    name="contentHash"
    type="string"
  >
    The hash of the file content, or version/revision number
  </ResponseField>
  <ResponseField
    name="lastModifiedDate"
    type="string (date-time)"
  >
    The last modified date of the file in RFC 3339 format
  </ResponseField>
  <ResponseField
    name="path"
    type="array"
    required
  >
    An array of the path's components sorted by level (root to leaf)
  </ResponseField>
</Expandable>

### Delete File

Deletes a file in Google Drive

**Input**:

<Expandable title="input fields">
  <ResponseField
    name="id"
    type="string"
    required
  >
    The ID of the Google Drive file
  </ResponseField>
</Expandable>

**Output**:

This Card has no output.

### Download file data

Download data from a file in Google Drive

**Input**:

<Expandable title="input fields">
  <ResponseField
    name="id"
    type="string"
    required
  >
    The ID of the Google Drive file whose content will be downloaded
  </ResponseField>
  <ResponseField
    name="index"
    type="boolean"
    required
  >
    Indicates if the file is to be indexed or not
  </ResponseField>
</Expandable>

**Output**:

<Expandable title="output">
  <ResponseField
    name="bpFileId"
    type="string"
    required
  >
    The Botpress file ID corresponding to the file that was uploaded from Google Drive to the Files API
  </ResponseField>
</Expandable>

### List items in folder

List the files and folders in a folder

**Input**:

<Expandable title="input fields">
  <ResponseField
    name="folderId"
    type="string"
  >
    The folder's ID. This could be a unique identifier from the external service, or a relative or absolute path, so long as it's unique.
  </ResponseField>
  <ResponseField
    name="filters"
    type="object"
  >
    Optional search filters

    <Expandable>
  <ResponseField
    name="itemType"
    type="enum: file, folder"
  >
    The entity type
  </ResponseField>
  <ResponseField
    name="maxSizeInBytes"
    type="number"
  >
    Filter the items by maximum size (in bytes)
  </ResponseField>
  <ResponseField
    name="modifiedAfter"
    type="string (date-time)"
  >
    Filter the items modified after the given date
  </ResponseField>
    </Expandable>
  </ResponseField>
  <ResponseField
    name="nextToken"
    type="string"
  >
    The token to get the next page of items. Leave empty to get the first page.
  </ResponseField>
</Expandable>

**Output**:

<Expandable title="output">
  <ResponseField
    name="items"
    type="array"
    required
  >
    The files and folders in the folder

    <Expandable title="file properties">
  <ResponseField
    name="id"
    type="string"
    required
  >
    The file's ID. This could be a unique identifier from the external service, or a relative or absolute path, so long as it's unique.
  </ResponseField>
  <ResponseField
    name="type"
    type="string"
    required
  >
    
  </ResponseField>
  <ResponseField
    name="name"
    type="string"
    required
  >
    The file's name. This will be displayed in the Botpress UI and be used as the file's name on Files API."
  </ResponseField>
  <ResponseField
    name="parentId"
    type="string"
  >
    The parent folder ID. Leave empty if the file is in the root folder.
  </ResponseField>
  <ResponseField
    name="absolutePath"
    type="string"
  >
    The absolute path of the file. Leave empty if not available.
  </ResponseField>
  <ResponseField
    name="sizeInBytes"
    type="number"
  >
    The file size in bytes, if available
  </ResponseField>
  <ResponseField
    name="lastModifiedDate"
    type="string (date-time)"
  >
    The last modified date of the file, if available
  </ResponseField>
  <ResponseField
    name="contentHash"
    type="string"
  >
    The hash of the file content, or version/revision number, if available
  </ResponseField>
    </Expandable>

    <Expandable title="folder properties">
  <ResponseField
    name="id"
    type="string"
    required
  >
    The folder's ID. This could be a unique identifier from the external service, or a relative or absolute path, so long as it's unique.
  </ResponseField>
  <ResponseField
    name="type"
    type="string"
    required
  >
    
  </ResponseField>
  <ResponseField
    name="name"
    type="string"
    required
  >
    The folder's name. This will be displayed in the Botpress UI and be used as the folder's name on Files API."
  </ResponseField>
  <ResponseField
    name="parentId"
    type="string"
  >
    The parent folder ID. Leave empty if the folder is in the root folder.
  </ResponseField>
  <ResponseField
    name="absolutePath"
    type="string"
  >
    The absolute path of the folder. Leave empty if not available.
  </ResponseField>
    </Expandable>
  </ResponseField>
  <ResponseField
    name="meta"
    type="object"
    required
  >
    

    <Expandable>
  <ResponseField
    name="nextToken"
    type="string"
  >
    The token to get the next page of items.
  </ResponseField>
    </Expandable>
  </ResponseField>
</Expandable>

### Transfer file to Botpress

Transfer a file from an external service to Botpress

**Input**:

<Expandable title="input fields">
  <ResponseField
    name="file"
    type="object"
    required
  >
    The file to transfer

    <Expandable>
  <ResponseField
    name="id"
    type="string"
    required
  >
    The file's ID. This could be a unique identifier from the external service, or a relative or absolute path, so long as it's unique.
  </ResponseField>
  <ResponseField
    name="type"
    type="string"
    required
  >
    
  </ResponseField>
  <ResponseField
    name="name"
    type="string"
    required
  >
    The file's name. This will be displayed in the Botpress UI and be used as the file's name on Files API."
  </ResponseField>
  <ResponseField
    name="parentId"
    type="string"
  >
    The parent folder ID. Leave empty if the file is in the root folder.
  </ResponseField>
  <ResponseField
    name="absolutePath"
    type="string"
  >
    The absolute path of the file. Leave empty if not available.
  </ResponseField>
  <ResponseField
    name="sizeInBytes"
    type="number"
  >
    The file size in bytes, if available
  </ResponseField>
  <ResponseField
    name="lastModifiedDate"
    type="string (date-time)"
  >
    The last modified date of the file, if available
  </ResponseField>
  <ResponseField
    name="contentHash"
    type="string"
  >
    The hash of the file content, or version/revision number, if available
  </ResponseField>
    </Expandable>
  </ResponseField>
  <ResponseField
    name="fileKey"
    type="string"
    required
  >
    The file key to use in Botpress
  </ResponseField>
  <ResponseField
    name="shouldIndex"
    type="boolean"
  >
    Whether to index the file in vector storage
  </ResponseField>
</Expandable>

**Output**:

<Expandable title="output">
  <ResponseField
    name="botpressFileId"
    type="string"
    required
  >
    The file ID of the uploaded file on Botpress
  </ResponseField>
</Expandable>

### List Files

List files in Google Drive

**Input**:

<Expandable title="input fields">
  <ResponseField
    name="nextToken"
    type="string"
  >
    The token to use to get the next page of results
  </ResponseField>
</Expandable>

**Output**:

<Expandable title="output">
  <ResponseField
    name="items"
    type="array"
    required
  >
    The list of items listed in Google Drive. Results may be paginated. If set, use nextToken to get additional results

    <Expandable title="array item properties">
  <ResponseField
    name="id"
    type="string"
    required
  >
    The ID of the Google Drive file
  </ResponseField>
  <ResponseField
    name="name"
    type="string"
    required
  >
    The name of the file
  </ResponseField>
  <ResponseField
    name="parentId"
    type="string"
  >
    The ID of the file that is the parent of this file. If not set, 'My Drive' is the parent
  </ResponseField>
  <ResponseField
    name="mimeType"
    type="string"
    required
  >
    The media type of the file
  </ResponseField>
  <ResponseField
    name="size"
    type="number"
    required
  >
    The size in bytes of the file
  </ResponseField>
  <ResponseField
    name="contentHash"
    type="string"
  >
    The hash of the file content, or version/revision number
  </ResponseField>
  <ResponseField
    name="lastModifiedDate"
    type="string (date-time)"
  >
    The last modified date of the file in RFC 3339 format
  </ResponseField>
  <ResponseField
    name="path"
    type="array"
    required
  >
    An array of the path's components sorted by level (root to leaf)
  </ResponseField>
    </Expandable>
  </ResponseField>
  <ResponseField
    name="meta"
    type="object"
    required
  >
    

    <Expandable>
  <ResponseField
    name="nextToken"
    type="string"
  >
    The token to pass as input to the next call of the list action to list additional items
  </ResponseField>
    </Expandable>
  </ResponseField>
</Expandable>

### List folders

List folders in Google Drive

**Input**:

<Expandable title="input fields">
  <ResponseField
    name="nextToken"
    type="string"
  >
    The token to use to get the next page of results
  </ResponseField>
</Expandable>

**Output**:

<Expandable title="output">
  <ResponseField
    name="items"
    type="array"
    required
  >
    The list of items listed in Google Drive. Results may be paginated. If set, use nextToken to get additional results

    <Expandable title="array item properties">
  <ResponseField
    name="id"
    type="string"
    required
  >
    The ID of the Google Drive file
  </ResponseField>
  <ResponseField
    name="name"
    type="string"
    required
  >
    The name of the file
  </ResponseField>
  <ResponseField
    name="parentId"
    type="string"
  >
    The ID of the file that is the parent of this file. If not set, 'My Drive' is the parent
  </ResponseField>
  <ResponseField
    name="mimeType"
    type="string"
    required
  >
    
  </ResponseField>
  <ResponseField
    name="path"
    type="array"
    required
  >
    An array of the path's components sorted by level (root to leaf)
  </ResponseField>
    </Expandable>
  </ResponseField>
  <ResponseField
    name="meta"
    type="object"
    required
  >
    

    <Expandable>
  <ResponseField
    name="nextToken"
    type="string"
  >
    The token to pass as input to the next call of the list action to list additional items
  </ResponseField>
    </Expandable>
  </ResponseField>
</Expandable>

### Read File

Read a file's metadata in a Google Drive

**Input**:

<Expandable title="input fields">
  <ResponseField
    name="id"
    type="string"
    required
  >
    The ID of the Google Drive file
  </ResponseField>
</Expandable>

**Output**:

<Expandable title="output">
  <ResponseField
    name="id"
    type="string"
    required
  >
    The ID of the Google Drive file
  </ResponseField>
  <ResponseField
    name="name"
    type="string"
    required
  >
    The name of the file
  </ResponseField>
  <ResponseField
    name="parentId"
    type="string"
  >
    The ID of the file that is the parent of this file. If not set, 'My Drive' is the parent
  </ResponseField>
  <ResponseField
    name="mimeType"
    type="string"
    required
  >
    The media type of the file
  </ResponseField>
  <ResponseField
    name="size"
    type="number"
    required
  >
    The size in bytes of the file
  </ResponseField>
  <ResponseField
    name="contentHash"
    type="string"
  >
    The hash of the file content, or version/revision number
  </ResponseField>
  <ResponseField
    name="lastModifiedDate"
    type="string (date-time)"
  >
    The last modified date of the file in RFC 3339 format
  </ResponseField>
  <ResponseField
    name="path"
    type="array"
    required
  >
    An array of the path's components sorted by level (root to leaf)
  </ResponseField>
</Expandable>

### Sync Channels

Sync channels for file change subscriptions

**Input**:

This Card has no input fields.

**Output**:

This Card has no output.

### Update File

Update a file's metadata in Google Drive

**Input**:

<Expandable title="input fields">
  <ResponseField
    name="id"
    type="string"
    required
  >
    The ID of the Google Drive file
  </ResponseField>
  <ResponseField
    name="name"
    type="string"
    required
  >
    The name of the file
  </ResponseField>
  <ResponseField
    name="parentId"
    type="string"
  >
    The ID of the file that is the parent of this file. If not set, 'My Drive' is the parent
  </ResponseField>
</Expandable>

**Output**:

<Expandable title="output">
  <ResponseField
    name="id"
    type="string"
    required
  >
    The ID of the Google Drive file
  </ResponseField>
  <ResponseField
    name="name"
    type="string"
    required
  >
    The name of the file
  </ResponseField>
  <ResponseField
    name="parentId"
    type="string"
  >
    The ID of the file that is the parent of this file. If not set, 'My Drive' is the parent
  </ResponseField>
  <ResponseField
    name="mimeType"
    type="string"
    required
  >
    The media type of the file
  </ResponseField>
  <ResponseField
    name="size"
    type="number"
    required
  >
    The size in bytes of the file
  </ResponseField>
  <ResponseField
    name="contentHash"
    type="string"
  >
    The hash of the file content, or version/revision number
  </ResponseField>
  <ResponseField
    name="lastModifiedDate"
    type="string (date-time)"
  >
    The last modified date of the file in RFC 3339 format
  </ResponseField>
  <ResponseField
    name="path"
    type="array"
    required
  >
    An array of the path's components sorted by level (root to leaf)
  </ResponseField>
</Expandable>

### Upload file data

Upload data to a file in Google Drive

**Input**:

<Expandable title="input fields">
  <ResponseField
    name="id"
    type="string"
    required
  >
    
  </ResponseField>
  <ResponseField
    name="url"
    type="string"
    required
  >
    The URL used to access the file whose content will be uploaded to Google Drive
  </ResponseField>
  <ResponseField
    name="mimeType"
    type="string"
  >
    Media type of the uploaded content. This will override any previously set media type
  </ResponseField>
</Expandable>

**Output**:

This Card has no output.


{/* vale on */}