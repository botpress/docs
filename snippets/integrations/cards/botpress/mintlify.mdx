{/* This file is auto-generated. Do not edit directly. */}
{/* vale off */}

Here's a reference for all [Cards](/learn/reference/cards/introduction) available with the integration:

### Create Agent job

<span>{"Creates a new agent job that can generate and edit documentation in a branch based on a prompt."}</span>

**Input**:

<Expandable title="input fields">
  <ResponseField
    name="prompt"
    type="string"
    required
  >
    <span>{"The prompt to send the Mintlify agent"}</span>
  </ResponseField>
  <ResponseField
    name="branchName"
    type="string"
    required
  >
    <span>{"The name of the branch the Mintlify agent will make changes on. If the branch doesn't exist, it'll automatically be created."}</span>
  </ResponseField>
</Expandable>

**Output**:

<Expandable title="output">
  <ResponseField
    name="response"
    type="string"
    required
  >
    <span>{"Streaming response containing the agent job execution details and results."}</span>
  </ResponseField>
</Expandable>

### Get Agent Job by ID

<span>{"Retrieve the details and status of a specific agent job by its ID."}</span>

**Input**:

<Expandable title="input fields">
  <ResponseField
    name="jobId"
    type="string"
    required
  >
    <span>{"The unique identifier of the agent job to retrieve"}</span>
  </ResponseField>
</Expandable>

**Output**:

<Expandable title="output">
  <ResponseField
    name="sessionId"
    type="string"
    required
  >
    <span>{"The ID of the session."}</span>
  </ResponseField>
  <ResponseField
    name="subdomain"
    type="string"
    required
  >
    <span>{"The subdomain this session belongs to"}</span>
  </ResponseField>
  <ResponseField
    name="branch"
    type="string | null"
    required
  >
    
  </ResponseField>
  <ResponseField
    name="haulted"
    type="boolean"
    required
  >
    <span>{"Whether the session execution was halted."}</span>
  </ResponseField>
  <ResponseField
    name="haultReason"
    type="string"
    required
  >
    <span>{"Reason for session halt."}</span>
  </ResponseField>
  <ResponseField
    name="pullRequestLink"
    type="string"
    required
  >
    <span>{"Link to the created pull request."}</span>
  </ResponseField>
  <ResponseField
    name="messageToUser"
    type="string"
    required
  >
    <span>{"Message for the user about the session outcome."}</span>
  </ResponseField>
  <ResponseField
    name="todos"
    type="array"
    required
  >
    <span>{"List of todo items from the session."}</span>

    <Expandable title="array item properties">
  <ResponseField
    name="content"
    type="string"
    required
  >
    <span>{"Brief description of the task."}</span>
  </ResponseField>
  <ResponseField
    name="status"
    type="string"
    required
  >
    <span>{"Current status of the task."}</span>
  </ResponseField>
  <ResponseField
    name="priority"
    type="string"
    required
  >
    <span>{"Priority level of the task"}</span>
  </ResponseField>
  <ResponseField
    name="id"
    type="string"
    required
  >
    <span>{"Unique identifier for the todo item."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>
  <ResponseField
    name="createdAt"
    type="string"
    required
  >
    <span>{"Timestamp when the session was created."}</span>
  </ResponseField>
</Expandable>

### Get All Agent Jobs

<span>{"Retrieve all agent jobs, including their status and details."}</span>

**Input**:

This Card has no input fields.

**Output**:

<Expandable title="output">
  <ResponseField
    name="allSessions"
    type="array"
    required
  >
    <span>{"Array of all agent sessions for the domain"}</span>

    <Expandable title="array item properties">
  <ResponseField
    name="sessionId"
    type="string"
    required
  >
    <span>{"The ID of the session."}</span>
  </ResponseField>
  <ResponseField
    name="subdomain"
    type="string"
    required
  >
    <span>{"The subdomain this session belongs to"}</span>
  </ResponseField>
  <ResponseField
    name="branch"
    type="string | null"
    required
  >
    
  </ResponseField>
  <ResponseField
    name="haulted"
    type="boolean"
    required
  >
    <span>{"Whether the session execution was halted."}</span>
  </ResponseField>
  <ResponseField
    name="haultReason"
    type="string"
    required
  >
    <span>{"Reason for session halt."}</span>
  </ResponseField>
  <ResponseField
    name="pullRequestLink"
    type="string"
    required
  >
    <span>{"Link to the created pull request."}</span>
  </ResponseField>
  <ResponseField
    name="messageToUser"
    type="string"
    required
  >
    <span>{"Message for the user about the session outcome."}</span>
  </ResponseField>
  <ResponseField
    name="todos"
    type="array"
    required
  >
    <span>{"List of todo items from the session."}</span>

    <Expandable title="array item properties">
  <ResponseField
    name="content"
    type="string"
    required
  >
    <span>{"Brief description of the task."}</span>
  </ResponseField>
  <ResponseField
    name="status"
    type="string"
    required
  >
    <span>{"Current status of the task."}</span>
  </ResponseField>
  <ResponseField
    name="priority"
    type="string"
    required
  >
    <span>{"Priority level of the task"}</span>
  </ResponseField>
  <ResponseField
    name="id"
    type="string"
    required
  >
    <span>{"Unique identifier for the todo item."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>
  <ResponseField
    name="createdAt"
    type="string"
    required
  >
    <span>{"Timestamp when the session was created."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>
</Expandable>


{/* vale on */}