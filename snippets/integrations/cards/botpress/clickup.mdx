{/* This file is auto-generated. Do not edit directly. */}
{/* vale off */}

Here's a reference for all [Cards](/learn/reference/cards/) available with the integration:

### Create a task

Create a new task in a list

**Input**:

<Expandable title="input fields">
  <ResponseField
    name="listId"
    type="string"
    required
  >
    ID of the list to create the task in
  </ResponseField>
  <ResponseField
    name="name"
    type="string"
    required
  >
    Name of the task to be created
  </ResponseField>
  <ResponseField
    name="description"
    type="string"
  >
    Description of the task to be created
  </ResponseField>
  <ResponseField
    name="status"
    type="string"
  >
    Status of the task to be created
  </ResponseField>
  <ResponseField
    name="assignees"
    type="array"
  >
    IDs of the assignees of the task to be created
  </ResponseField>
  <ResponseField
    name="dueDate"
    type="string (date-time)"
  >
    Due date of the task to be created
  </ResponseField>
  <ResponseField
    name="tags"
    type="array"
  >
    Tags of the task to be created
  </ResponseField>
</Expandable>

**Output**:

<Expandable title="output">
  <ResponseField
    name="taskId"
    type="string"
    required
  >
    Id of the task created
  </ResponseField>
</Expandable>

### Delete a task

Delete a task

**Input**:

<Expandable title="input fields">
  <ResponseField
    name="taskId"
    type="string"
    required
  >
    ID of the task to be deleted
  </ResponseField>
</Expandable>

**Output**:

This Card has no output.

### Get members of a list

Get all the members of a list

**Input**:

<Expandable title="input fields">
  <ResponseField
    name="listId"
    type="string"
    required
  >
    ID of the list to get the members of
  </ResponseField>
</Expandable>

**Output**:

<Expandable title="output">
  <ResponseField
    name="members"
    type="array"
    required
  >
    Members of the list

    <Expandable title="array item properties">
  <ResponseField
    name="id"
    type="number"
    required
  >
    ID of the member
  </ResponseField>
  <ResponseField
    name="username"
    type="string"
    required
  >
    Username of the member
  </ResponseField>
  <ResponseField
    name="email"
    type="string"
    required
  >
    Email of the member
  </ResponseField>
    </Expandable>
  </ResponseField>
</Expandable>

### Update a task

Update the details of a task

**Input**:

<Expandable title="input fields">
  <ResponseField
    name="taskId"
    type="string"
    required
  >
    ID of the task to be updated
  </ResponseField>
  <ResponseField
    name="name"
    type="string"
  >
    New name of the task
  </ResponseField>
  <ResponseField
    name="description"
    type="string"
  >
    New description of the task
  </ResponseField>
  <ResponseField
    name="status"
    type="string"
  >
    New status of the task
  </ResponseField>
  <ResponseField
    name="archived"
    type="boolean"
  >
    New archived status of the task
  </ResponseField>
  <ResponseField
    name="assigneesToAdd"
    type="array"
  >
    Members to add to the task
  </ResponseField>
  <ResponseField
    name="assigneesToRemove"
    type="array"
  >
    Members to remove from the task
  </ResponseField>
  <ResponseField
    name="dueDate"
    type="string (date-time)"
  >
    New due date of the task
  </ResponseField>
</Expandable>

**Output**:

<Expandable title="output">
  <ResponseField
    name="taskId"
    type="string"
    required
  >
    Id of the task updated
  </ResponseField>
</Expandable>


{/* vale on */}