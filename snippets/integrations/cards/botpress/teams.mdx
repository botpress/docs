{/* This file is auto-generated. Do not edit directly. */}
{/* vale off */}

Here's a reference for all [Cards](/learn/reference/cards/) available with the integration:

### Start DM Conversation

Initiate a conversation with a user in a DM by email or user ID.

**Input**:

<Expandable title="input fields">
  <ResponseField
    name="teamsUserId"
    type="string"
  >
    The ID of any Teams user from tenant to initiate the conversation with, eg: "29:2d5f3..."
  </ResponseField>
  <ResponseField
    name="teamsUserEmail"
    type="string"
  >
    The Email of any Teams user from tenant to initiate the conversation with
  </ResponseField>
  <ResponseField
    name="conversationId"
    type="string"
    required
  >
    The ID of any Botpress conversation from channel "Teams", eg: "conv_01JZ..."
  </ResponseField>
</Expandable>

**Output**:

<Expandable title="output">
  <ResponseField
    name="userId"
    type="string"
  >
    The ID of the user
  </ResponseField>
  <ResponseField
    name="conversationId"
    type="string"
    required
  >
    The ID of the new conversation
  </ResponseField>
</Expandable>

### Start Typing Indicator

**Input**:

<Expandable title="input fields">
  <ResponseField
    name="conversationId"
    type="string"
    required
  >
    
  </ResponseField>
  <ResponseField
    name="messageId"
    type="string"
    required
  >
    The message ID to which the typing indicator should be attached
  </ResponseField>
  <ResponseField
    name="timeout"
    type="number"
  >
    The timeout in milliseconds after which the typing indicator should stop
  </ResponseField>
</Expandable>

**Output**:

This Card has no output.

### Stop Typing Indicator

**Input**:

<Expandable title="input fields">
  <ResponseField
    name="conversationId"
    type="string"
    required
  >
    
  </ResponseField>
  <ResponseField
    name="messageId"
    type="string"
    required
  >
    The message ID from which the typing indicator should be removed
  </ResponseField>
</Expandable>

**Output**:

This Card has no output.


{/* vale on */}