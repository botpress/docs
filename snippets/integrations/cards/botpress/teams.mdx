{/* This file is auto-generated. Do not edit directly. */}
{/* vale off */}

Here's a reference for all [Cards](/learn/reference/cards/introduction) available with the integration:

### Start DM Conversation

<span>{"Initiate a conversation with a user in a DM by email or user ID."}</span>

**Input**:

<Expandable title="input fields">
  <ResponseField
    name="teamsUserId"
    type="string"
  >
    <span>{"The ID of any Teams user from tenant to initiate the conversation with, eg: \"29:2d5f3...\""}</span>
  </ResponseField>
  <ResponseField
    name="teamsUserEmail"
    type="string"
  >
    <span>{"The Email of any Teams user from tenant to initiate the conversation with"}</span>
  </ResponseField>
  <ResponseField
    name="conversationId"
    type="string"
    required
  >
    <span>{"The ID of any Botpress conversation from channel \"Teams\", eg: \"conv_01JZ...\""}</span>
  </ResponseField>
</Expandable>

**Output**:

<Expandable title="output">
  <ResponseField
    name="userId"
    type="string"
  >
    <span>{"The ID of the user"}</span>
  </ResponseField>
  <ResponseField
    name="conversationId"
    type="string"
    required
  >
    <span>{"The ID of the new conversation"}</span>
  </ResponseField>
</Expandable>

### Start Typing Indicator

**Input**:

<Expandable title="input fields">
  <ResponseField
    name="conversationId"
    type="string"
    required
  >
    
  </ResponseField>
  <ResponseField
    name="messageId"
    type="string"
    required
  >
    <span>{"The message ID to which the typing indicator should be attached"}</span>
  </ResponseField>
  <ResponseField
    name="timeout"
    type="number"
  >
    <span>{"The timeout in milliseconds after which the typing indicator should stop"}</span>
  </ResponseField>
</Expandable>

**Output**:

This Card has no output.

### Stop Typing Indicator

**Input**:

<Expandable title="input fields">
  <ResponseField
    name="conversationId"
    type="string"
    required
  >
    
  </ResponseField>
  <ResponseField
    name="messageId"
    type="string"
    required
  >
    <span>{"The message ID from which the typing indicator should be removed"}</span>
  </ResponseField>
</Expandable>

**Output**:

This Card has no output.


{/* vale on */}