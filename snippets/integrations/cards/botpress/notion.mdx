{/* This file is auto-generated. Do not edit directly. */}
{/* vale off */}

Here's a reference for all [Cards](/studio/concepts/cards/introduction) available with the integration:

### Add Comment to Discussion

<span>{"Add a comment to a discussion in Notion"}</span>

  <ResponseField
    name="input"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="discussionId"
    type="string"
    required
  >
    <span>{"The ID of the discussion to add the comment to. Can be found in the URL of the discussion"}</span>
  </ResponseField>
  <ResponseField
    name="commentBody"
    type="string"
    required
  >
    <span>{"Must be plain text"}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

  <ResponseField
    name="output"
    type="object"
  >
    This Card has no output.
  </ResponseField>

### Add Comment to Page

<span>{"Add a comment to a page in Notion"}</span>

  <ResponseField
    name="input"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="pageId"
    type="string"
    required
  >
    <span>{"The ID of the page to add the comment to. Can be found in the URL of the page"}</span>
  </ResponseField>
  <ResponseField
    name="commentBody"
    type="string"
    required
  >
    <span>{"Must be plain text"}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

  <ResponseField
    name="output"
    type="object"
  >
    This Card has no output.
  </ResponseField>

### Create Page in Database

<span>{"Add a new page to a database in Notion"}</span>

  <ResponseField
    name="input"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="databaseId"
    type="string"
    required
  >
    <span>{"The ID of the database to add the page to. Can be found in the URL of the database"}</span>
  </ResponseField>
  <ResponseField
    name="pageProperties"
    type="object"
    required
  >
    <span>{"The values of the page's properties. Must match the parent database's properties"}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

  <ResponseField
    name="output"
    type="object"
  >
    This Card has no output.
  </ResponseField>

### Delete Block

<span>{"Delete a block in Notion"}</span>

  <ResponseField
    name="input"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="blockId"
    type="string"
    required
  >
    <span>{"The ID of the block to delete. Can be found in the URL of the block"}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

  <ResponseField
    name="output"
    type="object"
  >
    This Card has no output.
  </ResponseField>

### Get Database

<span>{"Get a database from Notion"}</span>

  <ResponseField
    name="input"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="databaseId"
    type="string"
    required
  >
    <span>{"The ID of the database to fetch. Can be found in the URL of the database"}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

  <ResponseField
    name="output"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="object"
    type="string"
    required
  >
    <span>{"A stringified representation of the database"}</span>
  </ResponseField>
  <ResponseField
    name="properties"
    type="object"
    required
  >
    <span>{"Schema of properties for the database as they appear in Notion"}</span>
  </ResponseField>
  <ResponseField
    name="structure"
    type="string"
    required
  >
    <span>{"A stringified representation of the database structure"}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>


{/* vale on */}