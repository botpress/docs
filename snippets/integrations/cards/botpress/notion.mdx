{/* This file is auto-generated. Do not edit directly. */}
{/* vale off */}

Here's a reference for all [Cards](/studio/concepts/cards/introduction) available with the integration:

### Add Comment

<span>{"Add a comment to a page, block, or discussion in Notion"}</span>

  <ResponseField
    name="input"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="parentType"
    type="enum<string>"
    required
  >
    <span>{"The type of the parent"}</span>

Available options: `page`, `block`, `discussion`
  </ResponseField>
  <ResponseField
    name="parentId"
    type="string"
    required
  >
    <span>{"The ID of the parent to add the comment to. Can be found in the URL of the parent"}</span>
  </ResponseField>
  <ResponseField
    name="commentBody"
    type="string"
    required
  >
    <span>{"Must be plain text"}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

  <ResponseField
    name="output"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="commentId"
    type="string"
    required
  >
    <span>{"The ID of the comment that was created"}</span>
  </ResponseField>
  <ResponseField
    name="discussionId"
    type="string"
  >
    <span>{"The ID of the discussion that was created"}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

### Append Blocks to Page

<span>{"Append a markdown text to a page in Notion. The markdown text will be converted to notion blocks."}</span>

  <ResponseField
    name="input"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="pageId"
    type="string"
    required
  >
    <span>{"The ID of the page to append the blocks to. Can be found in the URL of the page"}</span>
  </ResponseField>
  <ResponseField
    name="markdownText"
    type="string"
    required
  >
    <span>{"The markdown text to append to the page"}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

  <ResponseField
    name="output"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="pageId"
    type="string"
    required
  >
    <span>{"The ID of the page where blocks were appended"}</span>
  </ResponseField>
  <ResponseField
    name="blockIds"
    type="array"
    required
  >
    <span>{"The IDs of the blocks that were created"}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

### Create Page

<span>{"Create a new page in Notion"}</span>

  <ResponseField
    name="input"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="parentType"
    type="enum<string>"
    required
  >
    <span>{"The type of the parent"}</span>

Available options: `data source`, `page`
  </ResponseField>
  <ResponseField
    name="parentId"
    type="string"
    required
  >
    <span>{"The ID of the parent to add the page to. Can be found in the URL of the parent"}</span>
  </ResponseField>
  <ResponseField
    name="title"
    type="string"
    required
  >
    <span>{"The title of the page"}</span>
  </ResponseField>
  <ResponseField
    name="dataSourceTitleName"
    type="string"
    default="Name"
  >
    <span>{"The name of the title property in the data source. If not provided, the default is \"Name\"."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

  <ResponseField
    name="output"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="pageId"
    type="string"
    required
  >
    <span>{"The ID of the page that was created"}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

### Delete Block

<span>{"Delete a block in Notion"}</span>

  <ResponseField
    name="input"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="blockId"
    type="string"
    required
  >
    <span>{"The ID of the block to delete. Can be found in the URL of the block"}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

  <ResponseField
    name="output"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="blockId"
    type="string"
    required
  >
    <span>{"The ID of the block that was deleted"}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

### Get Data Source

<span>{"Get a data source from Notion"}</span>

  <ResponseField
    name="input"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="dataSourceId"
    type="string"
    required
  >
    <span>{"The ID of the data source to fetch. Can be found in the URL of the data source"}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

  <ResponseField
    name="output"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="object"
    type="string"
    required
  >
    <span>{"A stringified representation of the data source"}</span>
  </ResponseField>
  <ResponseField
    name="properties"
    type="object"
    required
  >
    <span>{"Schema of properties for the data source as they appear in Notion"}</span>
  </ResponseField>
  <ResponseField
    name="pages"
    type="array"
    required
  >
    <span>{"List of pages in the data source"}</span>

    <Expandable title="array item properties">
  <ResponseField
    name="id"
    type="string"
    required
  >
    <span>{"The ID of the page"}</span>
  </ResponseField>
  <ResponseField
    name="title"
    type="string"
    required
  >
    <span>{"The title of the page"}</span>
  </ResponseField>
  <ResponseField
    name="pageProperties"
    type="object"
    required
  >
    <span>{"Schema of properties for the page as they appear in Notion"}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>
    </Expandable>
  </ResponseField>

### Get Database

<span>{"Get a database from Notion"}</span>

  <ResponseField
    name="input"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="databaseId"
    type="string"
    required
  >
    <span>{"The ID of the database to fetch. Can be found in the URL of the database"}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

  <ResponseField
    name="output"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="object"
    type="string"
  >
    <span>{"The type of object returned"}</span>
  </ResponseField>
  <ResponseField
    name="dataSources"
    type="array"
    required
  >
    <span>{"List of data sources in the database"}</span>

    <Expandable title="array item properties">
  <ResponseField
    name="id"
    type="string"
    required
  >
    <span>{"The ID of the data source"}</span>
  </ResponseField>
  <ResponseField
    name="name"
    type="string"
    required
  >
    <span>{"The name of the data source"}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>
    </Expandable>
  </ResponseField>

### Get Page

<span>{"Get a page from Notion"}</span>

  <ResponseField
    name="input"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="pageId"
    type="string"
    required
  >
    <span>{"The ID of the page to fetch. Can be found in the URL of the page"}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

  <ResponseField
    name="output"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="object"
    type="string"
  >
    <span>{"The type of object returned"}</span>
  </ResponseField>
  <ResponseField
    name="id"
    type="string"
  >
    <span>{"The ID of the object returned"}</span>
  </ResponseField>
  <ResponseField
    name="created_time"
    type="string"
  >
    <span>{"The time the object was created"}</span>
  </ResponseField>
  <ResponseField
    name="parent"
    type="object"
  >
    <span>{"The parent of the object"}</span>
  </ResponseField>
  <ResponseField
    name="created_by"
    type="object"
  >
    <span>{"The user who created the object"}</span>

    <Expandable>
  <ResponseField
    name="object"
    type="string"
  >
    <span>{"The type of object returned"}</span>
  </ResponseField>
  <ResponseField
    name="id"
    type="string"
  >
    <span>{"The ID of the object returned"}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>
  <ResponseField
    name="archived"
    type="boolean"
  >
    <span>{"Whether the object is archived"}</span>
  </ResponseField>
  <ResponseField
    name="properties"
    type="object"
  >
    <span>{"Schema of properties for the page as they appear in Notion"}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

### Get Page Content

<span>{"Get the content blocks of a page or block in Notion"}</span>

  <ResponseField
    name="input"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="pageId"
    type="string"
    required
  >
    <span>{"The ID of the page or block to fetch content from. Can be found in the URL"}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

  <ResponseField
    name="output"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="blocks"
    type="array"
    required
  >
    <span>{"Array of content blocks from the page"}</span>

    <Expandable title="array item properties">
  <ResponseField
    name="blockId"
    type="string"
    required
  >
    <span>{"The unique ID of the block"}</span>
  </ResponseField>
  <ResponseField
    name="parentId"
    type="string"
  >
    <span>{"The ID of the parent page or block"}</span>
  </ResponseField>
  <ResponseField
    name="type"
    type="string"
    required
  >
    <span>{"The type of the block (paragraph, heading_1, etc.)"}</span>
  </ResponseField>
  <ResponseField
    name="hasChildren"
    type="boolean"
    required
  >
    <span>{"Whether the block has nested child blocks"}</span>
  </ResponseField>
  <ResponseField
    name="richText"
    type="array"
    required
  >
    <span>{"The rich text content of the block with formatting information"}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>
    </Expandable>
  </ResponseField>

### Search by Title

<span>{"Search for pages and databases in Notion. Optionally filter by title. Only returns items that have been shared with the integration."}</span>

  <ResponseField
    name="input"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="title"
    type="string"
  >
    <span>{"Optional search query to match against page and database titles. If not provided, returns all accessible pages and databases."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

  <ResponseField
    name="output"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="results"
    type="array"
    required
  >
    <span>{"Array of pages and databases matching the search query"}</span>

    <Expandable title="array item properties">
  <ResponseField
    name="id"
    type="string"
    required
  >
    <span>{"The ID of the page or database"}</span>
  </ResponseField>
  <ResponseField
    name="title"
    type="string"
    required
  >
    <span>{"The title of the page or database"}</span>
  </ResponseField>
  <ResponseField
    name="type"
    type="string"
    required
  >
    <span>{"The type of the result (page or database)"}</span>
  </ResponseField>
  <ResponseField
    name="url"
    type="string"
    required
  >
    <span>{"The URL to the page or database"}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>
    </Expandable>
  </ResponseField>

### Update Page Properties

<span>{"Update one or more properties on a Notion page using raw Notion properties JSON"}</span>

  <ResponseField
    name="input"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="pageId"
    type="string"
    required
  >
    <span>{"The ID of the page to update. Can be found in the page URL"}</span>
  </ResponseField>
  <ResponseField
    name="propertiesJson"
    type="string"
    required
  >
    <span>{"Stringified JSON object for the Notion properties payload (same format as Notion pages.update API endpoint but without the \"properties\" key). Check the Notion API documentation for the correct format. https://developers.notion.com/reference/patch-page"}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

  <ResponseField
    name="output"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="pageId"
    type="string"
    required
  >
    <span>{"The updated page ID"}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>


{/* vale on */}