{/* This file is auto-generated. Do not edit directly. */}
{/* vale off */}

Here's a reference for all [Cards](/learn/reference/cards/) available with the integration:

### Start Conversation

Proactively starts a conversation with a WhatsApp user by sending them a message using a WhatsApp Message Template

**Input**:

<Expandable title="input fields">
  <ResponseField
    name="conversation"
    type="object"
    required
  >
    

    <Expandable>
  <ResponseField
    name="userPhone"
    type="string"
    required
  >
    Phone number of the WhatsApp user to start a conversation with
  </ResponseField>
  <ResponseField
    name="templateName"
    type="string"
    required
  >
    Name of the WhatsApp Message Template to start the conversation with
  </ResponseField>
  <ResponseField
    name="templateLanguage"
    type="string"
  >
    Language of the WhatsApp Message Template to start the conversation with. Defaults to "en" (English)
  </ResponseField>
  <ResponseField
    name="templateVariablesJson"
    type="string"
  >
    JSON array representation of variable values to pass to the WhatsApp Message Template (if required by the template)
  </ResponseField>
  <ResponseField
    name="botPhoneNumberId"
    type="string"
  >
    Phone number ID to use as sender (uses the default phone number ID if not provided)
  </ResponseField>
    </Expandable>
  </ResponseField>
</Expandable>

**Output**:

<Expandable title="output">
  <ResponseField
    name="conversationId"
    type="string"
    required
  >
    The Botpress ID of the created conversation
  </ResponseField>
</Expandable>

### Start Typing Indicator

**Input**:

<Expandable title="input fields">
  <ResponseField
    name="conversationId"
    type="string"
    required
  >
    
  </ResponseField>
  <ResponseField
    name="messageId"
    type="string"
    required
  >
    The message ID to which the typing indicator should be attached
  </ResponseField>
  <ResponseField
    name="timeout"
    type="number"
  >
    The timeout in milliseconds after which the typing indicator should stop
  </ResponseField>
</Expandable>

**Output**:

This Card has no output.

### Stop Typing Indicator

**Input**:

<Expandable title="input fields">
  <ResponseField
    name="conversationId"
    type="string"
    required
  >
    
  </ResponseField>
  <ResponseField
    name="messageId"
    type="string"
    required
  >
    The message ID from which the typing indicator should be removed
  </ResponseField>
</Expandable>

**Output**:

This Card has no output.


{/* vale on */}