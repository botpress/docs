{/* This file is auto-generated. Do not edit directly. */}
{/* vale off */}

Here's a reference for all [Cards](/learn/reference/cards/introduction) available with the integration:

### Start Conversation

<span>{"Proactively starts a conversation with a WhatsApp user by sending them a message using a WhatsApp Message Template"}</span>

**Input**:

<Expandable title="input fields">
  <ResponseField
    name="conversation"
    type="object"
    required
  >
    

    <Expandable>
  <ResponseField
    name="userPhone"
    type="string"
    required
  >
    <span>{"Phone number of the WhatsApp user to start a conversation with"}</span>
  </ResponseField>
  <ResponseField
    name="templateName"
    type="string"
    required
  >
    <span>{"Name of the WhatsApp Message Template to start the conversation with"}</span>
  </ResponseField>
  <ResponseField
    name="templateLanguage"
    type="string"
  >
    <span>{"Language of the WhatsApp Message Template to start the conversation with. Defaults to \"en\" (English)"}</span>
  </ResponseField>
  <ResponseField
    name="templateVariablesJson"
    type="string"
  >
    <span>{"JSON array representation of variable values to pass to the WhatsApp Message Template (if required by the template)"}</span>
  </ResponseField>
  <ResponseField
    name="botPhoneNumberId"
    type="string"
  >
    <span>{"Phone number ID to use as sender (uses the default phone number ID if not provided)"}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>
</Expandable>

**Output**:

<Expandable title="output">
  <ResponseField
    name="conversationId"
    type="string"
    required
  >
    <span>{"The Botpress ID of the created conversation"}</span>
  </ResponseField>
</Expandable>

### Start Typing Indicator

**Input**:

<Expandable title="input fields">
  <ResponseField
    name="conversationId"
    type="string"
    required
  >
    
  </ResponseField>
  <ResponseField
    name="messageId"
    type="string"
    required
  >
    <span>{"The message ID to which the typing indicator should be attached"}</span>
  </ResponseField>
  <ResponseField
    name="timeout"
    type="number"
  >
    <span>{"The timeout in milliseconds after which the typing indicator should stop"}</span>
  </ResponseField>
</Expandable>

**Output**:

This Card has no output.

### Stop Typing Indicator

**Input**:

<Expandable title="input fields">
  <ResponseField
    name="conversationId"
    type="string"
    required
  >
    
  </ResponseField>
  <ResponseField
    name="messageId"
    type="string"
    required
  >
    <span>{"The message ID from which the typing indicator should be removed"}</span>
  </ResponseField>
</Expandable>

**Output**:

This Card has no output.


{/* vale on */}