{/* This file is auto-generated. Do not edit directly. */}
{/* vale off */}

Here's a reference for all [Cards](/learn/reference/cards/) available with the integration:

### Change Task Priority

<span>{"Change the priority of a task"}</span>

**Input**:

<Expandable title="input fields">
  <ResponseField
    name="taskId"
    type="string"
    required
  >
    <span>{"The ID of the task"}</span>
  </ResponseField>
  <ResponseField
    name="priority"
    type="number"
    required
  >
    <span>{"Task priority from 1 (urgent) to 4 (lowest, default value)."}</span>
  </ResponseField>
</Expandable>

**Output**:

This Card has no output.

### Create New Task

<span>{"Create a new task"}</span>

**Input**:

<Expandable title="input fields">
  <ResponseField
    name="content"
    type="string"
    required
  >
    <span>{"The text of the task. This value may contain markdown-formatted text and hyperlinks."}</span>
  </ResponseField>
  <ResponseField
    name="description"
    type="string"
  >
    <span>{"A description for the task. This value may contain markdown-formatted text and hyperlinks."}</span>
  </ResponseField>
  <ResponseField
    name="projectId"
    type="string"
  >
    <span>{"The ID of the project."}</span>
  </ResponseField>
  <ResponseField
    name="sectionId"
    type="string"
  >
    <span>{"The ID of the section."}</span>
  </ResponseField>
  <ResponseField
    name="labelNames"
    type="array"
  >
    <span>{"The names of the labels to add to the task. If omitted, no label will be added."}</span>
  </ResponseField>
  <ResponseField
    name="parentTaskId"
    type="string"
  >
    <span>{"The ID of the task"}</span>
  </ResponseField>
  <ResponseField
    name="priority"
    type="number"
  >
    <span>{"Task priority from 1 (urgent) to 4 (lowest, default value)."}</span>
  </ResponseField>
  <ResponseField
    name="dueDate"
    type="string"
  >
    <span>{"The due date of the task. Must be in RFC3339 format in UTC. If omitted, the task will have no due date."}</span>
  </ResponseField>
</Expandable>

**Output**:

<Expandable title="output">
  <ResponseField
    name="taskId"
    type="string"
    required
  >
    <span>{"The ID of the task"}</span>
  </ResponseField>
</Expandable>

### Get All Projects

<span>{"Get all projects"}</span>

**Input**:

This Card has no input fields.

**Output**:

<Expandable title="output">
  <ResponseField
    name="projects"
    type="array"
    required
  >
    <span>{"All projects that are available to the user"}</span>

    <Expandable title="array item properties">
  <ResponseField
    name="id"
    type="string"
    required
  >
    <span>{"The ID of the project."}</span>
  </ResponseField>
  <ResponseField
    name="name"
    type="string"
    required
  >
    <span>{"The name of the project."}</span>
  </ResponseField>
  <ResponseField
    name="color"
    type="enum: berry_red, red, orange, yellow, olive_green, lime_green, green, mint_green, teal, sky_blue, light_blue, blue, grape, violet, lavender, magenta, salmon, charcoal, gunmetal, taupe"
    required
  >
    <span>{"The color of the project."}</span>
  </ResponseField>
  <ResponseField
    name="parentProjectId"
    type="string"
  >
    <span>{"The ID of the parent project. Omitted if the project has no parent project."}</span>
  </ResponseField>
  <ResponseField
    name="positionWithinParent"
    type="number"
    required
  >
    <span>{"Numerical index indicating project's order within its parent project (read-only). Will be 0 for inbox projects."}</span>
  </ResponseField>
  <ResponseField
    name="numberOfComments"
    type="number"
    required
  >
    <span>{"The number of comments on the project."}</span>
  </ResponseField>
  <ResponseField
    name="isShared"
    type="boolean"
    required
  >
    <span>{"Whether the project is shared or not."}</span>
  </ResponseField>
  <ResponseField
    name="isFavorite"
    type="boolean"
    required
  >
    <span>{"Whether the project is marked as favorite or not."}</span>
  </ResponseField>
  <ResponseField
    name="isInboxProject"
    type="boolean"
    required
  >
    <span>{"Whether the project is the user's inbox."}</span>
  </ResponseField>
  <ResponseField
    name="isTeamInbox"
    type="boolean"
    required
  >
    <span>{"Whether the project is a team inbox."}</span>
  </ResponseField>
  <ResponseField
    name="webUrl"
    type="string"
    required
  >
    <span>{"The URL of the project in the Todoist web app."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>
</Expandable>

### Get All Sections

<span>{"Get all sections in a project"}</span>

**Input**:

<Expandable title="input fields">
  <ResponseField
    name="projectId"
    type="string"
  >
    <span>{"The ID of the project."}</span>
  </ResponseField>
</Expandable>

**Output**:

<Expandable title="output">
  <ResponseField
    name="sections"
    type="array"
    required
  >
    <span>{"All sections in the project, or all sections in all projects if projectId is omitted"}</span>

    <Expandable title="array item properties">
  <ResponseField
    name="id"
    type="string"
    required
  >
    <span>{"The ID of the section."}</span>
  </ResponseField>
  <ResponseField
    name="name"
    type="string"
    required
  >
    <span>{"The name of the section."}</span>
  </ResponseField>
  <ResponseField
    name="projectId"
    type="string"
  >
    <span>{"The ID of the project this section belongs to."}</span>
  </ResponseField>
  <ResponseField
    name="positionWithinParent"
    type="number"
    required
  >
    <span>{"Numerical index indicating section's order within its parent project."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>
</Expandable>

### Get All Tasks

<span>{"Find tasks using optional filters"}</span>

**Input**:

<Expandable title="input fields">
  <ResponseField
    name="projectId"
    type="string"
  >
    <span>{"The ID of the project."}</span>
  </ResponseField>
  <ResponseField
    name="sectionId"
    type="string"
  >
    <span>{"The ID of the section."}</span>
  </ResponseField>
  <ResponseField
    name="labelName"
    type="string"
  >
    <span>{"The name of the label."}</span>
  </ResponseField>
</Expandable>

**Output**:

<Expandable title="output">
  <ResponseField
    name="tasks"
    type="array"
    required
  >
    <span>{"All matching tasks"}</span>

    <Expandable title="array item properties">
  <ResponseField
    name="id"
    type="string"
    required
  >
    <span>{"The ID of the task"}</span>
  </ResponseField>
  <ResponseField
    name="projectId"
    type="string"
    required
  >
    <span>{"The ID of the project this task belongs to (read-only)."}</span>
  </ResponseField>
  <ResponseField
    name="sectionId"
    type="string"
  >
    <span>{"The ID of the section this task belongs to (read-only). Omitted if the task is not in a section."}</span>
  </ResponseField>
  <ResponseField
    name="content"
    type="string"
    required
  >
    <span>{"The text of the task. This value may contain markdown-formatted text and hyperlinks."}</span>
  </ResponseField>
  <ResponseField
    name="description"
    type="string"
    required
  >
    <span>{"A description for the task. This value may contain markdown-formatted text and hyperlinks."}</span>
  </ResponseField>
  <ResponseField
    name="isCompleted"
    type="boolean"
    required
  >
    <span>{"Whether the task is completed or not."}</span>
  </ResponseField>
  <ResponseField
    name="labels"
    type="array"
    required
  >
    <span>{"The labels associated with the task. This is a list of names that may represent either personal or shared labels."}</span>
  </ResponseField>
  <ResponseField
    name="parentTaskId"
    type="string"
  >
    <span>{"The ID of the parent task (read-only). Omitted if the task has no parent task."}</span>
  </ResponseField>
  <ResponseField
    name="positionWithinParent"
    type="number"
    required
  >
    <span>{"Numerical index indicating task's order within its parent (task, or project for top-level tasks) (read-only)."}</span>
  </ResponseField>
  <ResponseField
    name="priority"
    type="number"
    required
  >
    <span>{"Task priority from 1 (urgent) to 4 (lowest, default value)."}</span>
  </ResponseField>
  <ResponseField
    name="dueDate"
    type="string"
  >
    <span>{"The due date of the task. Omitted if the task has no due date."}</span>
  </ResponseField>
  <ResponseField
    name="isRecurringDueDate"
    type="boolean"
    required
  >
    <span>{"Whether the due date is recurring or not."}</span>
  </ResponseField>
  <ResponseField
    name="webUrl"
    type="string"
    required
  >
    <span>{"URL to access this task in the Todoist web or mobile applications (read-only)."}</span>
  </ResponseField>
  <ResponseField
    name="numberOfComments"
    type="number"
    required
  >
    <span>{"The number of comments associated with the task (read-only)."}</span>
  </ResponseField>
  <ResponseField
    name="createdAt"
    type="string"
    required
  >
    <span>{"The date when the task was created (read-only)."}</span>
  </ResponseField>
  <ResponseField
    name="createdBy"
    type="string"
    required
  >
    <span>{"The ID of the user who created the task (read-only)."}</span>
  </ResponseField>
  <ResponseField
    name="assignee"
    type="string"
  >
    <span>{"The ID of the user to whom the task is assigned. Omitted if the task is not assigned."}</span>
  </ResponseField>
  <ResponseField
    name="assigner"
    type="string"
  >
    <span>{"The ID of the user who assigned the task (read-only). Omitted if the task is not assigned."}</span>
  </ResponseField>
  <ResponseField
    name="duration"
    type="object"
  >
    <span>{"The duration of the task. Omitted if the task has no duration."}</span>

    <Expandable>
  <ResponseField
    name="amount"
    type="number"
    required
  >
    <span>{"The amount of time."}</span>
  </ResponseField>
  <ResponseField
    name="unit"
    type="enum: minute, day"
    required
  >
    <span>{"The unit of time."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>
    </Expandable>
  </ResponseField>
</Expandable>

### Get Project ID

<span>{"Get the ID of the project"}</span>

**Input**:

<Expandable title="input fields">
  <ResponseField
    name="name"
    type="string"
    required
  >
    <span>{"The name of the project."}</span>
  </ResponseField>
</Expandable>

**Output**:

<Expandable title="output">
  <ResponseField
    name="projectId"
    type="string,null"
    required
  >
    <span>{"The ID of the project."}</span>
  </ResponseField>
</Expandable>

### Get Task ID

<span>{"Get the ID of the first task matching the given name"}</span>

**Input**:

<Expandable title="input fields">
  <ResponseField
    name="name"
    type="string"
    required
  >
    <span>{"The name of the task to search for"}</span>
  </ResponseField>
</Expandable>

**Output**:

<Expandable title="output">
  <ResponseField
    name="taskId"
    type="string,null"
    required
  >
    <span>{"The ID of the task"}</span>
  </ResponseField>
</Expandable>


{/* vale on */}