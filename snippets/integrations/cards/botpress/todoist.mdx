{/* This file is auto-generated. Do not edit directly. */}
{/* vale off */}

Here's a reference for all [Cards](/learn/reference/cards/) available with the integration:

### Change Task Priority

Change the priority of a task

**Input**:

<Expandable title="input fields">
  <ResponseField
    name="taskId"
    type="string"
    required
  >
    The ID of the task
  </ResponseField>
  <ResponseField
    name="priority"
    type="number"
    required
  >
    Task priority from 1 (urgent) to 4 (lowest, default value).
  </ResponseField>
</Expandable>

**Output**:

This Card has no output.

### Create New Task

Create a new task

**Input**:

<Expandable title="input fields">
  <ResponseField
    name="content"
    type="string"
    required
  >
    The text of the task. This value may contain markdown-formatted text and hyperlinks.
  </ResponseField>
  <ResponseField
    name="description"
    type="string"
  >
    A description for the task. This value may contain markdown-formatted text and hyperlinks.
  </ResponseField>
  <ResponseField
    name="projectId"
    type="string"
  >
    The ID of the project.
  </ResponseField>
  <ResponseField
    name="sectionId"
    type="string"
  >
    The ID of the section.
  </ResponseField>
  <ResponseField
    name="labelNames"
    type="array"
  >
    The names of the labels to add to the task. If omitted, no label will be added.
  </ResponseField>
  <ResponseField
    name="parentTaskId"
    type="string"
  >
    The ID of the task
  </ResponseField>
  <ResponseField
    name="priority"
    type="number"
  >
    Task priority from 1 (urgent) to 4 (lowest, default value).
  </ResponseField>
  <ResponseField
    name="dueDate"
    type="string"
  >
    The due date of the task. Must be in RFC3339 format in UTC. If omitted, the task will have no due date.
  </ResponseField>
</Expandable>

**Output**:

<Expandable title="output">
  <ResponseField
    name="taskId"
    type="string"
    required
  >
    The ID of the task
  </ResponseField>
</Expandable>

### Get All Projects

Get all projects

**Input**:

This Card has no input fields.

**Output**:

<Expandable title="output">
  <ResponseField
    name="projects"
    type="array"
    required
  >
    All projects that are available to the user

    <Expandable title="array item properties">
  <ResponseField
    name="id"
    type="string"
    required
  >
    The ID of the project.
  </ResponseField>
  <ResponseField
    name="name"
    type="string"
    required
  >
    The name of the project.
  </ResponseField>
  <ResponseField
    name="color"
    type="enum: berry_red, red, orange, yellow, olive_green, lime_green, green, mint_green, teal, sky_blue, light_blue, blue, grape, violet, lavender, magenta, salmon, charcoal, gunmetal, taupe"
    required
  >
    The color of the project.
  </ResponseField>
  <ResponseField
    name="parentProjectId"
    type="string"
  >
    The ID of the parent project. Omitted if the project has no parent project.
  </ResponseField>
  <ResponseField
    name="positionWithinParent"
    type="number"
    required
  >
    Numerical index indicating project's order within its parent project (read-only). Will be 0 for inbox projects.
  </ResponseField>
  <ResponseField
    name="numberOfComments"
    type="number"
    required
  >
    The number of comments on the project.
  </ResponseField>
  <ResponseField
    name="isShared"
    type="boolean"
    required
  >
    Whether the project is shared or not.
  </ResponseField>
  <ResponseField
    name="isFavorite"
    type="boolean"
    required
  >
    Whether the project is marked as favorite or not.
  </ResponseField>
  <ResponseField
    name="isInboxProject"
    type="boolean"
    required
  >
    Whether the project is the user's inbox.
  </ResponseField>
  <ResponseField
    name="isTeamInbox"
    type="boolean"
    required
  >
    Whether the project is a team inbox.
  </ResponseField>
  <ResponseField
    name="webUrl"
    type="string"
    required
  >
    The URL of the project in the Todoist web app.
  </ResponseField>
    </Expandable>
  </ResponseField>
</Expandable>

### Get All Sections

Get all sections in a project

**Input**:

<Expandable title="input fields">
  <ResponseField
    name="projectId"
    type="string"
  >
    The ID of the project.
  </ResponseField>
</Expandable>

**Output**:

<Expandable title="output">
  <ResponseField
    name="sections"
    type="array"
    required
  >
    All sections in the project, or all sections in all projects if projectId is omitted

    <Expandable title="array item properties">
  <ResponseField
    name="id"
    type="string"
    required
  >
    The ID of the section.
  </ResponseField>
  <ResponseField
    name="name"
    type="string"
    required
  >
    The name of the section.
  </ResponseField>
  <ResponseField
    name="projectId"
    type="string"
  >
    The ID of the project this section belongs to.
  </ResponseField>
  <ResponseField
    name="positionWithinParent"
    type="number"
    required
  >
    Numerical index indicating section's order within its parent project.
  </ResponseField>
    </Expandable>
  </ResponseField>
</Expandable>

### Get All Tasks

Find tasks using optional filters

**Input**:

<Expandable title="input fields">
  <ResponseField
    name="projectId"
    type="string"
  >
    The ID of the project.
  </ResponseField>
  <ResponseField
    name="sectionId"
    type="string"
  >
    The ID of the section.
  </ResponseField>
  <ResponseField
    name="labelName"
    type="string"
  >
    The name of the label.
  </ResponseField>
</Expandable>

**Output**:

<Expandable title="output">
  <ResponseField
    name="tasks"
    type="array"
    required
  >
    All matching tasks

    <Expandable title="array item properties">
  <ResponseField
    name="id"
    type="string"
    required
  >
    The ID of the task
  </ResponseField>
  <ResponseField
    name="projectId"
    type="string"
    required
  >
    The ID of the project this task belongs to (read-only).
  </ResponseField>
  <ResponseField
    name="sectionId"
    type="string"
  >
    The ID of the section this task belongs to (read-only). Omitted if the task is not in a section.
  </ResponseField>
  <ResponseField
    name="content"
    type="string"
    required
  >
    The text of the task. This value may contain markdown-formatted text and hyperlinks.
  </ResponseField>
  <ResponseField
    name="description"
    type="string"
    required
  >
    A description for the task. This value may contain markdown-formatted text and hyperlinks.
  </ResponseField>
  <ResponseField
    name="isCompleted"
    type="boolean"
    required
  >
    Whether the task is completed or not.
  </ResponseField>
  <ResponseField
    name="labels"
    type="array"
    required
  >
    The labels associated with the task. This is a list of names that may represent either personal or shared labels.
  </ResponseField>
  <ResponseField
    name="parentTaskId"
    type="string"
  >
    The ID of the parent task (read-only). Omitted if the task has no parent task.
  </ResponseField>
  <ResponseField
    name="positionWithinParent"
    type="number"
    required
  >
    Numerical index indicating task's order within its parent (task, or project for top-level tasks) (read-only).
  </ResponseField>
  <ResponseField
    name="priority"
    type="number"
    required
  >
    Task priority from 1 (urgent) to 4 (lowest, default value).
  </ResponseField>
  <ResponseField
    name="dueDate"
    type="string"
  >
    The due date of the task. Omitted if the task has no due date.
  </ResponseField>
  <ResponseField
    name="isRecurringDueDate"
    type="boolean"
    required
  >
    Whether the due date is recurring or not.
  </ResponseField>
  <ResponseField
    name="webUrl"
    type="string"
    required
  >
    URL to access this task in the Todoist web or mobile applications (read-only).
  </ResponseField>
  <ResponseField
    name="numberOfComments"
    type="number"
    required
  >
    The number of comments associated with the task (read-only).
  </ResponseField>
  <ResponseField
    name="createdAt"
    type="string"
    required
  >
    The date when the task was created (read-only).
  </ResponseField>
  <ResponseField
    name="createdBy"
    type="string"
    required
  >
    The ID of the user who created the task (read-only).
  </ResponseField>
  <ResponseField
    name="assignee"
    type="string"
  >
    The ID of the user to whom the task is assigned. Omitted if the task is not assigned.
  </ResponseField>
  <ResponseField
    name="assigner"
    type="string"
  >
    The ID of the user who assigned the task (read-only). Omitted if the task is not assigned.
  </ResponseField>
  <ResponseField
    name="duration"
    type="object"
  >
    The duration of the task. Omitted if the task has no duration.

    <Expandable>
  <ResponseField
    name="amount"
    type="number"
    required
  >
    The amount of time.
  </ResponseField>
  <ResponseField
    name="unit"
    type="enum: minute, day"
    required
  >
    The unit of time.
  </ResponseField>
    </Expandable>
  </ResponseField>
    </Expandable>
  </ResponseField>
</Expandable>

### Get Project ID

Get the ID of the project

**Input**:

<Expandable title="input fields">
  <ResponseField
    name="name"
    type="string"
    required
  >
    The name of the project.
  </ResponseField>
</Expandable>

**Output**:

<Expandable title="output">
  <ResponseField
    name="projectId"
    type="string,null"
    required
  >
    The ID of the project.
  </ResponseField>
</Expandable>

### Get Task ID

Get the ID of the first task matching the given name

**Input**:

<Expandable title="input fields">
  <ResponseField
    name="name"
    type="string"
    required
  >
    The name of the task to search for
  </ResponseField>
</Expandable>

**Output**:

<Expandable title="output">
  <ResponseField
    name="taskId"
    type="string,null"
    required
  >
    The ID of the task
  </ResponseField>
</Expandable>


{/* vale on */}