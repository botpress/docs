{/* This file is auto-generated. Do not edit directly. */}
{/* vale off */}

Here's a reference for all [Cards](/studio/concepts/cards/introduction) available with the integration:

### Change Message Labels

<span>{"Modifies the labels on the specified message by adding or removing label IDs."}</span>

  <ResponseField
    name="input"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="id"
    type="string"
    required
  >
    <span>{"The ID of the message to modify."}</span>
  </ResponseField>
  <ResponseField
    name="addLabelIds"
    type="array"
  >
    <span>{"A list of IDs of labels to add to this message."}</span>
  </ResponseField>
  <ResponseField
    name="removeLabelIds"
    type="array"
  >
    <span>{"A list of IDs of labels to remove from this message."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

  <ResponseField
    name="output"
    type="object"
  >
    This Card has no output.
  </ResponseField>

### Create Draft

<span>{"Creates a new draft with the specified email content."}</span>

  <ResponseField
    name="input"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="to"
    type="string"
    required
  >
    <span>{"The recipient email address."}</span>
  </ResponseField>
  <ResponseField
    name="subject"
    type="string"
    required
  >
    <span>{"The subject of the email."}</span>
  </ResponseField>
  <ResponseField
    name="body"
    type="string"
    required
  >
    <span>{"The body content of the email."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

  <ResponseField
    name="output"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="id"
    type="string"
  >
    <span>{"The immutable ID of the created draft."}</span>
  </ResponseField>
  <ResponseField
    name="message"
    type="object"
  >
    <span>{"The message content of the draft."}</span>

    <Expandable>
  <ResponseField
    name="id"
    type="string"
  >
    <span>{"The ID of the message."}</span>
  </ResponseField>
  <ResponseField
    name="threadId"
    type="string"
  >
    <span>{"The ID of the thread."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>
    </Expandable>
  </ResponseField>

### Create Label

<span>{"Creates a new label in the user's mailbox."}</span>

  <ResponseField
    name="input"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="name"
    type="string"
    required
  >
    <span>{"The display name of the label to create."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

  <ResponseField
    name="output"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="id"
    type="string"
  >
    <span>{"The immutable ID of the created label."}</span>
  </ResponseField>
  <ResponseField
    name="name"
    type="string"
  >
    <span>{"The display name of the label."}</span>
  </ResponseField>
  <ResponseField
    name="type"
    type="string"
  >
    <span>{"The owner type for the label (system or user)."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

### Delete Draft

<span>{"Immediately and permanently deletes the specified draft. This operation cannot be undone."}</span>

  <ResponseField
    name="input"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="id"
    type="string"
    required
  >
    <span>{"The ID of the draft to delete."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

  <ResponseField
    name="output"
    type="object"
  >
    This Card has no output.
  </ResponseField>

### Delete Label

<span>{"Immediately and permanently deletes the specified label. Messages and threads are not deleted, they simply lose this label."}</span>

  <ResponseField
    name="input"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="id"
    type="string"
    required
  >
    <span>{"The ID of the label to delete."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

  <ResponseField
    name="output"
    type="object"
  >
    This Card has no output.
  </ResponseField>

### Delete Message

<span>{"Immediately and permanently deletes the specified message using its ID. This operation cannot be undone. Prefer messages.trash instead"}</span>

  <ResponseField
    name="input"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="id"
    type="string"
    required
  >
    <span>{"The ID of the message to delete."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

  <ResponseField
    name="output"
    type="object"
  >
    This Card has no output.
  </ResponseField>

### Get Draft

<span>{"Gets the specified draft by its ID."}</span>

  <ResponseField
    name="input"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="id"
    type="string"
    required
  >
    <span>{"The ID of the draft to retrieve."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

  <ResponseField
    name="output"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="id"
    type="string"
  >
    <span>{"The immutable ID of the draft."}</span>
  </ResponseField>
  <ResponseField
    name="message"
    type="object"
  >
    <span>{"The message content of the draft."}</span>

    <Expandable>
  <ResponseField
    name="id"
    type="string"
  >
    <span>{"The ID of the message."}</span>
  </ResponseField>
  <ResponseField
    name="threadId"
    type="string"
  >
    <span>{"The ID of the thread."}</span>
  </ResponseField>
  <ResponseField
    name="labelIds"
    type="array"
  >
    <span>{"List of label IDs applied to the draft message."}</span>
  </ResponseField>
  <ResponseField
    name="snippet"
    type="string"
  >
    <span>{"A short part of the message text."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>
    </Expandable>
  </ResponseField>

### Get Label

<span>{"Gets the specified label by its ID."}</span>

  <ResponseField
    name="input"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="id"
    type="string"
    required
  >
    <span>{"The ID of the label to retrieve."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

  <ResponseField
    name="output"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="id"
    type="string"
  >
    <span>{"The immutable ID of the label."}</span>
  </ResponseField>
  <ResponseField
    name="name"
    type="string"
  >
    <span>{"The display name of the label."}</span>
  </ResponseField>
  <ResponseField
    name="type"
    type="string"
  >
    <span>{"The owner type for the label (system or user)."}</span>
  </ResponseField>
  <ResponseField
    name="messageListVisibility"
    type="string"
  >
    <span>{"The visibility of messages with this label in the message list."}</span>
  </ResponseField>
  <ResponseField
    name="labelListVisibility"
    type="string"
  >
    <span>{"The visibility of the label in the label list."}</span>
  </ResponseField>
  <ResponseField
    name="messagesTotal"
    type="number"
  >
    <span>{"The total number of messages with the label."}</span>
  </ResponseField>
  <ResponseField
    name="messagesUnread"
    type="number"
  >
    <span>{"The number of unread messages with the label."}</span>
  </ResponseField>
  <ResponseField
    name="threadsTotal"
    type="number"
  >
    <span>{"The total number of threads with the label."}</span>
  </ResponseField>
  <ResponseField
    name="threadsUnread"
    type="number"
  >
    <span>{"The number of unread threads with the label."}</span>
  </ResponseField>
  <ResponseField
    name="color"
    type="object"
  >
    <span>{"The color to assign to the label."}</span>

    <Expandable>
  <ResponseField
    name="backgroundColor"
    type="string"
  >
    <span>{"The background color."}</span>
  </ResponseField>
  <ResponseField
    name="textColor"
    type="string"
  >
    <span>{"The text color."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>
    </Expandable>
  </ResponseField>

### Get Message Attachment

<span>{"Gets the specified message attachment by its ID."}</span>

  <ResponseField
    name="input"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="messageId"
    type="string"
    required
  >
    <span>{"The ID of the message containing the attachment."}</span>
  </ResponseField>
  <ResponseField
    name="attachmentId"
    type="string"
    required
  >
    <span>{"The ID of the attachment to retrieve."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

  <ResponseField
    name="output"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="attachment"
    type="object"
    required
  >
    <span>{"The attachment retrieved from the message."}</span>

    <Expandable>
  <ResponseField
    name="size"
    type="number | null"
  >
    
  </ResponseField>
  <ResponseField
    name="data"
    type="string | null"
  >
    
  </ResponseField>
  <ResponseField
    name="attachmentId"
    type="string | null"
  >
    
  </ResponseField>
    </Expandable>
  </ResponseField>
    </Expandable>
  </ResponseField>

### Get Message Attachment From Mail

<span>{"Gets the first attachment from a message by automatically finding the attachment ID from the message."}</span>

  <ResponseField
    name="input"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="messageId"
    type="string"
    required
  >
    <span>{"The ID of the message containing the attachment."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

  <ResponseField
    name="output"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="attachment"
    type="object"
    required
  >
    <span>{"The attachment retrieved from the message."}</span>

    <Expandable>
  <ResponseField
    name="size"
    type="number | null"
  >
    
  </ResponseField>
  <ResponseField
    name="data"
    type="string | null"
  >
    
  </ResponseField>
  <ResponseField
    name="attachmentId"
    type="string | null"
  >
    
  </ResponseField>
    </Expandable>
  </ResponseField>
    </Expandable>
  </ResponseField>

### Get Thread

<span>{"Gets the specified thread by its ID, including all messages in the thread."}</span>

  <ResponseField
    name="input"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="id"
    type="string"
    required
  >
    <span>{"The ID of the thread to retrieve."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

  <ResponseField
    name="output"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="id"
    type="string"
  >
    <span>{"The unique ID of the thread."}</span>
  </ResponseField>
  <ResponseField
    name="snippet"
    type="string"
  >
    <span>{"A short part of the message text."}</span>
  </ResponseField>
  <ResponseField
    name="historyId"
    type="string"
  >
    <span>{"The history ID of the thread."}</span>
  </ResponseField>
  <ResponseField
    name="messages"
    type="array"
  >
    <span>{"The list of messages in the thread."}</span>

    <Expandable title="array item properties">
  <ResponseField
    name="id"
    type="string"
  >
    <span>{"The ID of the message."}</span>
  </ResponseField>
  <ResponseField
    name="threadId"
    type="string"
  >
    <span>{"The ID of the thread the message belongs to."}</span>
  </ResponseField>
  <ResponseField
    name="labelIds"
    type="array"
  >
    <span>{"List of label IDs applied to this message."}</span>
  </ResponseField>
  <ResponseField
    name="snippet"
    type="string"
  >
    <span>{"A short part of the message text."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>
    </Expandable>
  </ResponseField>

### List Drafts

<span>{"Lists all drafts in the user's mailbox."}</span>

  <ResponseField
    name="input"
    type="object"
  >
    This Card has no input fields.
  </ResponseField>

  <ResponseField
    name="output"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="drafts"
    type="array"
  >
    <span>{"List of drafts."}</span>

    <Expandable title="array item properties">
  <ResponseField
    name="id"
    type="string"
  >
    <span>{"The immutable ID of the draft."}</span>
  </ResponseField>
  <ResponseField
    name="message"
    type="object"
  >
    <span>{"The message content of the draft."}</span>

    <Expandable>
  <ResponseField
    name="id"
    type="string"
  >
    <span>{"The ID of the message contained in the draft."}</span>
  </ResponseField>
  <ResponseField
    name="threadId"
    type="string"
  >
    <span>{"The ID of the thread the draft belongs to."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>
    </Expandable>
  </ResponseField>
  <ResponseField
    name="resultSizeEstimate"
    type="number"
  >
    <span>{"Estimated total number of results."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

### List Labels

<span>{"Lists all labels in the user's mailbox, including system labels and user-created labels."}</span>

  <ResponseField
    name="input"
    type="object"
  >
    This Card has no input fields.
  </ResponseField>

  <ResponseField
    name="output"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="labels"
    type="array"
  >
    <span>{"List of labels."}</span>

    <Expandable title="array item properties">
  <ResponseField
    name="id"
    type="string"
  >
    <span>{"The immutable ID of the label."}</span>
  </ResponseField>
  <ResponseField
    name="name"
    type="string"
  >
    <span>{"The display name of the label."}</span>
  </ResponseField>
  <ResponseField
    name="type"
    type="string"
  >
    <span>{"The owner type for the label (system or user)."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>
    </Expandable>
  </ResponseField>

### List Threads

<span>{"Lists all email threads in the user's mailbox."}</span>

  <ResponseField
    name="input"
    type="object"
  >
    This Card has no input fields.
  </ResponseField>

  <ResponseField
    name="output"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="threads"
    type="array"
  >
    <span>{"List of threads in the mailbox."}</span>

    <Expandable title="array item properties">
  <ResponseField
    name="id"
    type="string"
  >
    <span>{"The unique ID of the thread."}</span>
  </ResponseField>
  <ResponseField
    name="snippet"
    type="string"
  >
    <span>{"A short part of the message text."}</span>
  </ResponseField>
  <ResponseField
    name="historyId"
    type="string"
  >
    <span>{"The history ID of the thread."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>
  <ResponseField
    name="resultSizeEstimate"
    type="number"
  >
    <span>{"Estimated total number of results."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

### Send Draft

<span>{"Sends the specified draft. The draft will be deleted after being sent."}</span>

  <ResponseField
    name="input"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="id"
    type="string"
    required
  >
    <span>{"The ID of the draft to send."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

  <ResponseField
    name="output"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="id"
    type="string"
  >
    <span>{"The ID of the sent message."}</span>
  </ResponseField>
  <ResponseField
    name="threadId"
    type="string"
  >
    <span>{"The ID of the thread the message belongs to."}</span>
  </ResponseField>
  <ResponseField
    name="labelIds"
    type="array"
  >
    <span>{"List of label IDs applied to the sent message."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

### Trash Message

<span>{"Moves the specified message to the trash. The message can be restored from the trash using untrashMessage."}</span>

  <ResponseField
    name="input"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="id"
    type="string"
    required
  >
    <span>{"The ID of the message to trash."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

  <ResponseField
    name="output"
    type="object"
  >
    This Card has no output.
  </ResponseField>

### Trash Thread

<span>{"Moves the specified thread to the trash. All messages in the thread will be moved to trash. The thread can be restored using untrashThread."}</span>

  <ResponseField
    name="input"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="id"
    type="string"
    required
  >
    <span>{"The ID of the thread to trash."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

  <ResponseField
    name="output"
    type="object"
  >
    This Card has no output.
  </ResponseField>

### Untrash Message

<span>{"Removes the specified message from the trash and restores it to the inbox."}</span>

  <ResponseField
    name="input"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="id"
    type="string"
    required
  >
    <span>{"The ID of the message to untrash."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

  <ResponseField
    name="output"
    type="object"
  >
    This Card has no output.
  </ResponseField>

### Untrash Thread

<span>{"Removes the specified thread from the trash. All messages in the thread will be restored."}</span>

  <ResponseField
    name="input"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="id"
    type="string"
    required
  >
    <span>{"The ID of the thread to restore from trash."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

  <ResponseField
    name="output"
    type="object"
  >
    This Card has no output.
  </ResponseField>

### Update Draft

<span>{"Replaces a draft's content with the new email content provided."}</span>

  <ResponseField
    name="input"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="id"
    type="string"
    required
  >
    <span>{"The ID of the draft to update."}</span>
  </ResponseField>
  <ResponseField
    name="to"
    type="string"
    required
  >
    <span>{"The recipient email address."}</span>
  </ResponseField>
  <ResponseField
    name="subject"
    type="string"
    required
  >
    <span>{"The subject of the email."}</span>
  </ResponseField>
  <ResponseField
    name="body"
    type="string"
    required
  >
    <span>{"The body content of the email."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

  <ResponseField
    name="output"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="id"
    type="string"
  >
    <span>{"The immutable ID of the updated draft."}</span>
  </ResponseField>
  <ResponseField
    name="message"
    type="object"
  >
    <span>{"The message content of the draft."}</span>

    <Expandable>
  <ResponseField
    name="id"
    type="string"
  >
    <span>{"The ID of the message."}</span>
  </ResponseField>
  <ResponseField
    name="threadId"
    type="string"
  >
    <span>{"The ID of the thread."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>
    </Expandable>
  </ResponseField>

### Update Label

<span>{"Updates the specified label with new properties like name or color."}</span>

  <ResponseField
    name="input"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="id"
    type="string"
    required
  >
    <span>{"The ID of the label to update."}</span>
  </ResponseField>
  <ResponseField
    name="name"
    type="string"
  >
    <span>{"The new display name for the label."}</span>
  </ResponseField>
  <ResponseField
    name="backgroundColor"
    type="string"
  >
    <span>{"The background color as hex string (e.g., #ffffff)."}</span>
  </ResponseField>
  <ResponseField
    name="textColor"
    type="string"
  >
    <span>{"The text color as hex string (e.g., #000000)."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

  <ResponseField
    name="output"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="id"
    type="string"
  >
    <span>{"The immutable ID of the label."}</span>
  </ResponseField>
  <ResponseField
    name="name"
    type="string"
  >
    <span>{"The display name of the label."}</span>
  </ResponseField>
  <ResponseField
    name="type"
    type="string"
  >
    <span>{"The owner type for the label (system or user)."}</span>
  </ResponseField>
  <ResponseField
    name="color"
    type="object"
  >
    <span>{"The color assigned to the label."}</span>

    <Expandable>
  <ResponseField
    name="backgroundColor"
    type="string"
  >
    <span>{"The background color."}</span>
  </ResponseField>
  <ResponseField
    name="textColor"
    type="string"
  >
    <span>{"The text color."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>
    </Expandable>
  </ResponseField>


{/* vale on */}