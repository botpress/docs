{/* This file is auto-generated. Do not edit directly. */}
{/* vale off */}

Here's a reference for all [Cards](/studio/concepts/cards/introduction) available with the integration:

### Create Contact

<span>{"Create an Odoo contact. Call listContactFields first unless the needed res.partner field names were already retrieved in this conversation."}</span>

  <ResponseField
    name="input"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="values"
    type="object"
    required
  >
    <span>{"Odoo contact field values keyed by field name. Use listContactFields before using custom fields; company names are usually represented by name or parent_id, not company_name."}</span>

    <Expandable>
  <ResponseField
    name="name"
    type="string"
    required
  >
    <span>{"Contact or company display name."}</span>
  </ResponseField>
  <ResponseField
    name="email"
    type="string"
  >
    <span>{"Contact email address."}</span>
  </ResponseField>
  <ResponseField
    name="phone"
    type="string"
  >
    <span>{"Contact phone number."}</span>
  </ResponseField>
  <ResponseField
    name="parent_id"
    type="number"
  >
    <span>{"Related company/contact ID for this contact."}</span>
  </ResponseField>
  <ResponseField
    name="is_company"
    type="boolean"
  >
    <span>{"Whether this contact represents a company."}</span>
  </ResponseField>
  <ResponseField
    name="street"
    type="string"
  >
    <span>{"Street address."}</span>
  </ResponseField>
  <ResponseField
    name="city"
    type="string"
  >
    <span>{"City."}</span>
  </ResponseField>
  <ResponseField
    name="zip"
    type="string"
  >
    <span>{"Postal or ZIP code."}</span>
  </ResponseField>
  <ResponseField
    name="country_id"
    type="number"
  >
    <span>{"Related Odoo country ID."}</span>
  </ResponseField>
  <ResponseField
    name="state_id"
    type="number"
  >
    <span>{"Related Odoo state ID."}</span>
  </ResponseField>
  <ResponseField
    name="vat"
    type="string"
  >
    <span>{"Tax identification number."}</span>
  </ResponseField>
  <ResponseField
    name="website"
    type="string"
  >
    <span>{"Contact or company website."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>
  <ResponseField
    name="context"
    type="object"
  >
    <span>{"Optional Odoo context values to pass with the request."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

  <ResponseField
    name="output"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="id"
    type="number"
    required
  >
    <span>{"ID of the created Odoo contact."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

### Create Lead

<span>{"Create an Odoo CRM lead or opportunity. Call listLeadFields first unless the needed crm.lead field names were already retrieved in this conversation."}</span>

  <ResponseField
    name="input"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="values"
    type="object"
    required
  >
    <span>{"Odoo CRM lead field values keyed by field name."}</span>

    <Expandable>
  <ResponseField
    name="name"
    type="string"
    required
  >
    <span>{"Lead or opportunity title."}</span>
  </ResponseField>
  <ResponseField
    name="email_from"
    type="string"
  >
    <span>{"Lead email address."}</span>
  </ResponseField>
  <ResponseField
    name="phone"
    type="string"
  >
    <span>{"Lead phone number."}</span>
  </ResponseField>
  <ResponseField
    name="mobile"
    type="string"
  >
    <span>{"Lead mobile phone number."}</span>
  </ResponseField>
  <ResponseField
    name="contact_name"
    type="string"
  >
    <span>{"Name of the person associated with the lead."}</span>
  </ResponseField>
  <ResponseField
    name="partner_name"
    type="string"
  >
    <span>{"Company name associated with the lead."}</span>
  </ResponseField>
  <ResponseField
    name="partner_id"
    type="number"
  >
    <span>{"Related Odoo customer/contact ID."}</span>
  </ResponseField>
  <ResponseField
    name="stage_id"
    type="number"
  >
    <span>{"Odoo CRM stage ID."}</span>
  </ResponseField>
  <ResponseField
    name="user_id"
    type="number"
  >
    <span>{"Assigned Odoo user ID."}</span>
  </ResponseField>
  <ResponseField
    name="team_id"
    type="number"
  >
    <span>{"Assigned Odoo sales team ID."}</span>
  </ResponseField>
  <ResponseField
    name="type"
    type="enum<string>"
  >
    <span>{"Whether the CRM record is a lead or opportunity."}</span>

Available options: `lead`, `opportunity`
  </ResponseField>
  <ResponseField
    name="probability"
    type="number"
  >
    <span>{"Success probability percentage."}</span>
  </ResponseField>
  <ResponseField
    name="expected_revenue"
    type="number"
  >
    <span>{"Expected revenue for the opportunity."}</span>
  </ResponseField>
  <ResponseField
    name="description"
    type="string"
  >
    <span>{"Internal notes or description for the lead."}</span>
  </ResponseField>
  <ResponseField
    name="referred"
    type="string"
  >
    <span>{"Referral source text."}</span>
  </ResponseField>
  <ResponseField
    name="source_id"
    type="number"
  >
    <span>{"Odoo UTM source ID."}</span>
  </ResponseField>
  <ResponseField
    name="medium_id"
    type="number"
  >
    <span>{"Odoo UTM medium ID."}</span>
  </ResponseField>
  <ResponseField
    name="campaign_id"
    type="number"
  >
    <span>{"Odoo UTM campaign ID."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>
  <ResponseField
    name="context"
    type="object"
  >
    <span>{"Optional Odoo context values to pass with the request."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

  <ResponseField
    name="output"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="id"
    type="number"
    required
  >
    <span>{"ID of the created Odoo CRM lead."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

### Create Ticket

<span>{"Create an Odoo helpdesk ticket. Call listTicketFields first unless the needed helpdesk.ticket field names were already retrieved in this conversation."}</span>

  <ResponseField
    name="input"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="values"
    type="object"
    required
  >
    <span>{"Odoo helpdesk ticket field values keyed by field name."}</span>

    <Expandable>
  <ResponseField
    name="name"
    type="string"
    required
  >
    <span>{"Helpdesk ticket title."}</span>
  </ResponseField>
  <ResponseField
    name="description"
    type="string"
  >
    <span>{"Ticket description or customer request details."}</span>
  </ResponseField>
  <ResponseField
    name="partner_id"
    type="number"
  >
    <span>{"Related Odoo customer/contact ID."}</span>
  </ResponseField>
  <ResponseField
    name="partner_name"
    type="string"
  >
    <span>{"Customer name for the ticket."}</span>
  </ResponseField>
  <ResponseField
    name="partner_email"
    type="string"
  >
    <span>{"Customer email address for the ticket."}</span>
  </ResponseField>
  <ResponseField
    name="email_cc"
    type="string"
  >
    <span>{"Additional email recipients copied on the ticket."}</span>
  </ResponseField>
  <ResponseField
    name="team_id"
    type="number"
  >
    <span>{"Assigned Odoo helpdesk team ID."}</span>
  </ResponseField>
  <ResponseField
    name="user_id"
    type="number"
  >
    <span>{"Assigned Odoo user ID."}</span>
  </ResponseField>
  <ResponseField
    name="stage_id"
    type="number"
  >
    <span>{"Odoo helpdesk ticket stage ID."}</span>
  </ResponseField>
  <ResponseField
    name="ticket_type_id"
    type="number"
  >
    <span>{"Odoo helpdesk ticket type ID."}</span>
  </ResponseField>
  <ResponseField
    name="priority"
    type="enum<string>"
  >
    <span>{"Odoo ticket priority, from 0 (lowest) to 3 (highest)."}</span>

Available options: `0`, `1`, `2`, `3`
  </ResponseField>
  <ResponseField
    name="tag_ids"
    type="array"
  >
    <span>{"Odoo helpdesk ticket tag IDs."}</span>
  </ResponseField>
  <ResponseField
    name="company_id"
    type="number"
  >
    <span>{"Related Odoo company ID."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>
  <ResponseField
    name="context"
    type="object"
  >
    <span>{"Optional Odoo context values to pass with the request."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

  <ResponseField
    name="output"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="id"
    type="number"
    required
  >
    <span>{"ID of the created Odoo helpdesk ticket."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

### Delete Contacts

<span>{"Delete one or more Odoo contacts owned by the specified Odoo user."}</span>

  <ResponseField
    name="input"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="ids"
    type="array"
    required
  >
    <span>{"Odoo contact record IDs."}</span>
  </ResponseField>
  <ResponseField
    name="ownerId"
    type="number"
    required
  >
    <span>{"Odoo user ID that must match each contact owner before the contact is deleted."}</span>
  </ResponseField>
  <ResponseField
    name="context"
    type="object"
  >
    <span>{"Optional Odoo context values to pass with the request."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

  <ResponseField
    name="output"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="deletedIds"
    type="array"
    required
  >
    <span>{"Odoo contact record IDs that were deleted."}</span>
  </ResponseField>
  <ResponseField
    name="notDeletedContacts"
    type="array"
    required
  >
    <span>{"Odoo contacts that could not be deleted, with the reason for each contact."}</span>

    <Expandable title="array item properties">
  <ResponseField
    name="id"
    type="number"
    required
  >
    <span>{"Odoo contact record ID."}</span>
  </ResponseField>
  <ResponseField
    name="name"
    type="string"
  >
    <span>{"Odoo contact display name."}</span>
  </ResponseField>
  <ResponseField
    name="reason"
    type="string"
    required
  >
    <span>{"Why the contact could not be deleted."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>
    </Expandable>
  </ResponseField>

### Delete Leads

<span>{"Delete one or more Odoo CRM leads owned by the specified Odoo user."}</span>

  <ResponseField
    name="input"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="ids"
    type="array"
    required
  >
    <span>{"Odoo CRM lead record IDs."}</span>
  </ResponseField>
  <ResponseField
    name="ownerId"
    type="number"
    required
  >
    <span>{"Odoo user ID that must match each lead owner before the lead is deleted."}</span>
  </ResponseField>
  <ResponseField
    name="context"
    type="object"
  >
    <span>{"Optional Odoo context values to pass with the request."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

  <ResponseField
    name="output"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="deletedIds"
    type="array"
    required
  >
    <span>{"Odoo CRM lead record IDs that were deleted."}</span>
  </ResponseField>
  <ResponseField
    name="notDeletedLeads"
    type="array"
    required
  >
    <span>{"Odoo CRM leads that could not be deleted, with the reason for each lead."}</span>

    <Expandable title="array item properties">
  <ResponseField
    name="id"
    type="number"
    required
  >
    <span>{"Odoo CRM lead record ID."}</span>
  </ResponseField>
  <ResponseField
    name="name"
    type="string"
  >
    <span>{"Odoo CRM lead display name."}</span>
  </ResponseField>
  <ResponseField
    name="reason"
    type="string"
    required
  >
    <span>{"Why the lead could not be deleted."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>
    </Expandable>
  </ResponseField>

### Delete Tickets

<span>{"Delete one or more Odoo helpdesk tickets."}</span>

  <ResponseField
    name="input"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="ids"
    type="array"
    required
  >
    <span>{"Odoo helpdesk ticket record IDs."}</span>
  </ResponseField>
  <ResponseField
    name="context"
    type="object"
  >
    <span>{"Optional Odoo context values to pass with the request."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

  <ResponseField
    name="output"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="deletedIds"
    type="array"
    required
  >
    <span>{"Odoo helpdesk ticket record IDs that were deleted."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

### Get Current User

<span>{"Get the Odoo user ID associated with the configured API key."}</span>

  <ResponseField
    name="input"
    type="object"
  >
    This Card has no input fields.
  </ResponseField>

  <ResponseField
    name="output"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="id"
    type="number"
    required
  >
    <span>{"Odoo user ID associated with the configured API key."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

### List Odoo Contact Fields

<span>{"List available fields for Odoo contacts. Call this before searchContacts, listContacts, createContact, or updateContacts when selecting Odoo contact field names."}</span>

  <ResponseField
    name="input"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="allfields"
    type="array"
  >
    <span>{"Odoo res.partner field names to include in the response. Call listContactFields before choosing fields unless the exact field names were already retrieved in this conversation. Do not request CRM lead fields here; use searchLeads or listLeadFields for crm.lead fields."}</span>
  </ResponseField>
  <ResponseField
    name="attributes"
    type="array"
  >
    <span>{"Field metadata attributes to return, such as string, type, and required."}</span>
  </ResponseField>
  <ResponseField
    name="context"
    type="object"
  >
    <span>{"Optional Odoo context values to pass with the request."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

  <ResponseField
    name="output"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="fields"
    type="object"
    required
  >
    <span>{"Field metadata keyed by Odoo field name."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

### List Contacts

<span>{"Read Odoo contacts by ID. Call listContactFields first unless the needed res.partner field names were already retrieved in this conversation."}</span>

  <ResponseField
    name="input"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="ids"
    type="array"
    required
  >
    <span>{"Odoo contact record IDs."}</span>
  </ResponseField>
  <ResponseField
    name="fields"
    type="array"
  >
    <span>{"Odoo res.partner field names to include in the response. Call listContactFields before choosing fields unless the exact field names were already retrieved in this conversation. Do not request CRM lead fields here; use searchLeads or listLeadFields for crm.lead fields."}</span>
  </ResponseField>
  <ResponseField
    name="context"
    type="object"
  >
    <span>{"Optional Odoo context values to pass with the request."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

  <ResponseField
    name="output"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="records"
    type="array"
    required
  >
    <span>{"Requested Odoo contact records."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

### List Odoo Lead Fields

<span>{"List available fields for Odoo CRM leads. Call this before searchLeads, listLeads, createLead, or updateLeads when selecting Odoo lead field names."}</span>

  <ResponseField
    name="input"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="allfields"
    type="array"
  >
    <span>{"Odoo crm.lead field names to include in the response. Call listLeadFields before choosing fields unless the exact field names were already retrieved in this conversation. To find which contacts are also leads, first retrieve the available contact and lead fields, then read comparable fields."}</span>
  </ResponseField>
  <ResponseField
    name="attributes"
    type="array"
  >
    <span>{"Field metadata attributes to return, such as string, type, and required."}</span>
  </ResponseField>
  <ResponseField
    name="context"
    type="object"
  >
    <span>{"Optional Odoo context values to pass with the request."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

  <ResponseField
    name="output"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="fields"
    type="object"
    required
  >
    <span>{"Field metadata keyed by Odoo lead field name."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

### List Leads

<span>{"Read Odoo CRM leads by ID. Call listLeadFields first unless the needed crm.lead field names were already retrieved in this conversation."}</span>

  <ResponseField
    name="input"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="ids"
    type="array"
    required
  >
    <span>{"Odoo CRM lead record IDs."}</span>
  </ResponseField>
  <ResponseField
    name="fields"
    type="array"
  >
    <span>{"Odoo crm.lead field names to include in the response. Call listLeadFields before choosing fields unless the exact field names were already retrieved in this conversation. To find which contacts are also leads, first retrieve the available contact and lead fields, then read comparable fields."}</span>
  </ResponseField>
  <ResponseField
    name="context"
    type="object"
  >
    <span>{"Optional Odoo context values to pass with the request."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

  <ResponseField
    name="output"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="records"
    type="array"
    required
  >
    <span>{"Requested Odoo CRM lead records."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

### List Odoo Ticket Fields

<span>{"List available fields for Odoo helpdesk tickets."}</span>

  <ResponseField
    name="input"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="allfields"
    type="array"
  >
    <span>{"Odoo helpdesk.ticket field names to include in the response. Call listTicketFields before choosing fields unless the exact field names were already retrieved in this conversation."}</span>
  </ResponseField>
  <ResponseField
    name="attributes"
    type="array"
  >
    <span>{"Field metadata attributes to return, such as string, type, and required."}</span>
  </ResponseField>
  <ResponseField
    name="context"
    type="object"
  >
    <span>{"Optional Odoo context values to pass with the request."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

  <ResponseField
    name="output"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="fields"
    type="object"
    required
  >
    <span>{"Field metadata keyed by Odoo ticket field name."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

### List Tickets

<span>{"List Odoo helpdesk tickets by ID. Call listTicketFields first unless the needed helpdesk.ticket field names were already retrieved in this conversation."}</span>

  <ResponseField
    name="input"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="ids"
    type="array"
    required
  >
    <span>{"Odoo helpdesk ticket record IDs."}</span>
  </ResponseField>
  <ResponseField
    name="fields"
    type="array"
  >
    <span>{"Odoo helpdesk.ticket field names to include in the response. Call listTicketFields before choosing fields unless the exact field names were already retrieved in this conversation."}</span>
  </ResponseField>
  <ResponseField
    name="context"
    type="object"
  >
    <span>{"Optional Odoo context values to pass with the request."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

  <ResponseField
    name="output"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="records"
    type="array"
    required
  >
    <span>{"Requested Odoo helpdesk ticket records."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

### Search Contacts

<span>{"Search Odoo contacts using an Odoo domain and optional read parameters. Call listContactFields first unless the needed res.partner field names were already retrieved in this conversation."}</span>

  <ResponseField
    name="input"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="domain"
    type="array"
  >
    <span>{"Odoo domain filters, such as [[\"id\", \"in\", [1, 2, 3]]]."}</span>
  </ResponseField>
  <ResponseField
    name="fields"
    type="array"
  >
    <span>{"Odoo res.partner field names to include in the response. Call listContactFields before choosing fields unless the exact field names were already retrieved in this conversation. Do not request CRM lead fields here; use searchLeads or listLeadFields for crm.lead fields."}</span>
  </ResponseField>
  <ResponseField
    name="offset"
    type="number"
  >
    <span>{"Number of matching contacts to skip."}</span>
  </ResponseField>
  <ResponseField
    name="limit"
    type="number"
  >
    <span>{"Maximum number of contacts to return."}</span>
  </ResponseField>
  <ResponseField
    name="order"
    type="string"
  >
    <span>{"Odoo order expression, such as \"name asc\"."}</span>
  </ResponseField>
  <ResponseField
    name="context"
    type="object"
  >
    <span>{"Optional Odoo context values to pass with the request."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

  <ResponseField
    name="output"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="records"
    type="array"
    required
  >
    <span>{"Matching Odoo contact records."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

### Search Leads

<span>{"Search Odoo CRM leads using an Odoo domain and optional read parameters. Call listLeadFields first unless the needed crm.lead field names were already retrieved in this conversation."}</span>

  <ResponseField
    name="input"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="domain"
    type="array"
  >
    <span>{"Odoo domain filters, such as [[\"id\", \"in\", [1, 2, 3]]]."}</span>
  </ResponseField>
  <ResponseField
    name="fields"
    type="array"
  >
    <span>{"Odoo crm.lead field names to include in the response. Call listLeadFields before choosing fields unless the exact field names were already retrieved in this conversation. To find which contacts are also leads, first retrieve the available contact and lead fields, then read comparable fields."}</span>
  </ResponseField>
  <ResponseField
    name="offset"
    type="number"
  >
    <span>{"Number of matching leads to skip."}</span>
  </ResponseField>
  <ResponseField
    name="limit"
    type="number"
  >
    <span>{"Maximum number of leads to return."}</span>
  </ResponseField>
  <ResponseField
    name="order"
    type="string"
  >
    <span>{"Odoo order expression, such as \"name asc\"."}</span>
  </ResponseField>
  <ResponseField
    name="context"
    type="object"
  >
    <span>{"Optional Odoo context values to pass with the request."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

  <ResponseField
    name="output"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="records"
    type="array"
    required
  >
    <span>{"Matching Odoo CRM lead records."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

### Search Tickets

<span>{"Search Odoo helpdesk tickets using an Odoo domain and optional read parameters. Call listTicketFields first unless the needed helpdesk.ticket field names were already retrieved in this conversation."}</span>

  <ResponseField
    name="input"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="domain"
    type="array"
  >
    <span>{"Odoo domain filters, such as [[\"id\", \"in\", [1, 2, 3]]]."}</span>
  </ResponseField>
  <ResponseField
    name="fields"
    type="array"
  >
    <span>{"Odoo helpdesk.ticket field names to include in the response. Call listTicketFields before choosing fields unless the exact field names were already retrieved in this conversation."}</span>
  </ResponseField>
  <ResponseField
    name="offset"
    type="number"
  >
    <span>{"Number of matching tickets to skip."}</span>
  </ResponseField>
  <ResponseField
    name="limit"
    type="number"
  >
    <span>{"Maximum number of tickets to return."}</span>
  </ResponseField>
  <ResponseField
    name="order"
    type="string"
  >
    <span>{"Odoo order expression, such as \"name asc\"."}</span>
  </ResponseField>
  <ResponseField
    name="context"
    type="object"
  >
    <span>{"Optional Odoo context values to pass with the request."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

  <ResponseField
    name="output"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="records"
    type="array"
    required
  >
    <span>{"Matching Odoo helpdesk ticket records."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

### Update Contacts

<span>{"Update one or more Odoo contacts. Call listContactFields first unless the needed res.partner field names were already retrieved in this conversation."}</span>

  <ResponseField
    name="input"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="ids"
    type="array"
    required
  >
    <span>{"Odoo contact record IDs."}</span>
  </ResponseField>
  <ResponseField
    name="values"
    type="object"
    required
  >
    <span>{"Odoo contact field values to update, keyed by field name."}</span>

    <Expandable>
  <ResponseField
    name="name"
    type="string"
  >
    <span>{"Contact or company display name."}</span>
  </ResponseField>
  <ResponseField
    name="email"
    type="string"
  >
    <span>{"Contact email address."}</span>
  </ResponseField>
  <ResponseField
    name="phone"
    type="string"
  >
    <span>{"Contact phone number."}</span>
  </ResponseField>
  <ResponseField
    name="parent_id"
    type="number"
  >
    <span>{"Related company/contact ID for this contact."}</span>
  </ResponseField>
  <ResponseField
    name="is_company"
    type="boolean"
  >
    <span>{"Whether this contact represents a company."}</span>
  </ResponseField>
  <ResponseField
    name="street"
    type="string"
  >
    <span>{"Street address."}</span>
  </ResponseField>
  <ResponseField
    name="city"
    type="string"
  >
    <span>{"City."}</span>
  </ResponseField>
  <ResponseField
    name="zip"
    type="string"
  >
    <span>{"Postal or ZIP code."}</span>
  </ResponseField>
  <ResponseField
    name="country_id"
    type="number"
  >
    <span>{"Related Odoo country ID."}</span>
  </ResponseField>
  <ResponseField
    name="state_id"
    type="number"
  >
    <span>{"Related Odoo state ID."}</span>
  </ResponseField>
  <ResponseField
    name="vat"
    type="string"
  >
    <span>{"Tax identification number."}</span>
  </ResponseField>
  <ResponseField
    name="website"
    type="string"
  >
    <span>{"Contact or company website."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>
  <ResponseField
    name="context"
    type="object"
  >
    <span>{"Optional Odoo context values to pass with the request."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

  <ResponseField
    name="output"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="updatedIds"
    type="array"
    required
  >
    <span>{"Odoo contact record IDs that were updated."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

### Update Leads

<span>{"Update one or more Odoo CRM leads. Call listLeadFields first unless the needed crm.lead field names were already retrieved in this conversation."}</span>

  <ResponseField
    name="input"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="ids"
    type="array"
    required
  >
    <span>{"Odoo CRM lead record IDs."}</span>
  </ResponseField>
  <ResponseField
    name="values"
    type="object"
    required
  >
    <span>{"Odoo CRM lead field values to update, keyed by field name."}</span>

    <Expandable>
  <ResponseField
    name="name"
    type="string"
  >
    <span>{"Lead or opportunity title."}</span>
  </ResponseField>
  <ResponseField
    name="email_from"
    type="string"
  >
    <span>{"Lead email address."}</span>
  </ResponseField>
  <ResponseField
    name="phone"
    type="string"
  >
    <span>{"Lead phone number."}</span>
  </ResponseField>
  <ResponseField
    name="mobile"
    type="string"
  >
    <span>{"Lead mobile phone number."}</span>
  </ResponseField>
  <ResponseField
    name="contact_name"
    type="string"
  >
    <span>{"Name of the person associated with the lead."}</span>
  </ResponseField>
  <ResponseField
    name="partner_name"
    type="string"
  >
    <span>{"Company name associated with the lead."}</span>
  </ResponseField>
  <ResponseField
    name="partner_id"
    type="number"
  >
    <span>{"Related Odoo customer/contact ID."}</span>
  </ResponseField>
  <ResponseField
    name="stage_id"
    type="number"
  >
    <span>{"Odoo CRM stage ID."}</span>
  </ResponseField>
  <ResponseField
    name="user_id"
    type="number"
  >
    <span>{"Assigned Odoo user ID."}</span>
  </ResponseField>
  <ResponseField
    name="team_id"
    type="number"
  >
    <span>{"Assigned Odoo sales team ID."}</span>
  </ResponseField>
  <ResponseField
    name="type"
    type="enum<string>"
  >
    <span>{"Whether the CRM record is a lead or opportunity."}</span>

Available options: `lead`, `opportunity`
  </ResponseField>
  <ResponseField
    name="probability"
    type="number"
  >
    <span>{"Success probability percentage."}</span>
  </ResponseField>
  <ResponseField
    name="expected_revenue"
    type="number"
  >
    <span>{"Expected revenue for the opportunity."}</span>
  </ResponseField>
  <ResponseField
    name="description"
    type="string"
  >
    <span>{"Internal notes or description for the lead."}</span>
  </ResponseField>
  <ResponseField
    name="referred"
    type="string"
  >
    <span>{"Referral source text."}</span>
  </ResponseField>
  <ResponseField
    name="source_id"
    type="number"
  >
    <span>{"Odoo UTM source ID."}</span>
  </ResponseField>
  <ResponseField
    name="medium_id"
    type="number"
  >
    <span>{"Odoo UTM medium ID."}</span>
  </ResponseField>
  <ResponseField
    name="campaign_id"
    type="number"
  >
    <span>{"Odoo UTM campaign ID."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>
  <ResponseField
    name="context"
    type="object"
  >
    <span>{"Optional Odoo context values to pass with the request."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

  <ResponseField
    name="output"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="updatedIds"
    type="array"
    required
  >
    <span>{"Odoo CRM lead record IDs that were updated."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

### Update Tickets

<span>{"Update one or more Odoo helpdesk tickets. Call listTicketFields first unless the needed helpdesk.ticket field names were already retrieved in this conversation."}</span>

  <ResponseField
    name="input"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="ids"
    type="array"
    required
  >
    <span>{"Odoo helpdesk ticket record IDs."}</span>
  </ResponseField>
  <ResponseField
    name="values"
    type="object"
    required
  >
    <span>{"Odoo helpdesk ticket field values to update, keyed by field name."}</span>

    <Expandable>
  <ResponseField
    name="name"
    type="string"
  >
    <span>{"Helpdesk ticket title."}</span>
  </ResponseField>
  <ResponseField
    name="description"
    type="string"
  >
    <span>{"Ticket description or customer request details."}</span>
  </ResponseField>
  <ResponseField
    name="partner_id"
    type="number"
  >
    <span>{"Related Odoo customer/contact ID."}</span>
  </ResponseField>
  <ResponseField
    name="partner_name"
    type="string"
  >
    <span>{"Customer name for the ticket."}</span>
  </ResponseField>
  <ResponseField
    name="partner_email"
    type="string"
  >
    <span>{"Customer email address for the ticket."}</span>
  </ResponseField>
  <ResponseField
    name="email_cc"
    type="string"
  >
    <span>{"Additional email recipients copied on the ticket."}</span>
  </ResponseField>
  <ResponseField
    name="team_id"
    type="number"
  >
    <span>{"Assigned Odoo helpdesk team ID."}</span>
  </ResponseField>
  <ResponseField
    name="user_id"
    type="number"
  >
    <span>{"Assigned Odoo user ID."}</span>
  </ResponseField>
  <ResponseField
    name="stage_id"
    type="number"
  >
    <span>{"Odoo helpdesk ticket stage ID."}</span>
  </ResponseField>
  <ResponseField
    name="ticket_type_id"
    type="number"
  >
    <span>{"Odoo helpdesk ticket type ID."}</span>
  </ResponseField>
  <ResponseField
    name="priority"
    type="enum<string>"
  >
    <span>{"Odoo ticket priority, from 0 (lowest) to 3 (highest)."}</span>

Available options: `0`, `1`, `2`, `3`
  </ResponseField>
  <ResponseField
    name="tag_ids"
    type="array"
  >
    <span>{"Odoo helpdesk ticket tag IDs."}</span>
  </ResponseField>
  <ResponseField
    name="company_id"
    type="number"
  >
    <span>{"Related Odoo company ID."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>
  <ResponseField
    name="context"
    type="object"
  >
    <span>{"Optional Odoo context values to pass with the request."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>

  <ResponseField
    name="output"
    type="object"
  >
    <Expandable>
  <ResponseField
    name="updatedIds"
    type="array"
    required
  >
    <span>{"Odoo helpdesk ticket record IDs that were updated."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>


{/* vale on */}