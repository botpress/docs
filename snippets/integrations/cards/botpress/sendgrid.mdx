{/* This file is auto-generated. Do not edit directly. */}
{/* vale off */}

Here's a reference for all [Cards](/learn/reference/cards/) available with the integration:

### Send Email

<span>{"Sends an email to the specified client"}</span>

**Input**:

<Expandable title="input fields">
  <ResponseField
    name="from"
    type="string (email)"
    required
  >
    <span>{"The email address of the correspondent (e.g. example@example.com)"}</span>
  </ResponseField>
  <ResponseField
    name="to"
    type="string (email)"
    required
  >
    <span>{"The email address of the correspondent (e.g. example@example.com)"}</span>
  </ResponseField>
  <ResponseField
    name="cc"
    type="array"
  >
    <span>{"List of carbon copy recipients"}</span>
  </ResponseField>
  <ResponseField
    name="bcc"
    type="array"
  >
    <span>{"List of blind carbon copy recipients"}</span>
  </ResponseField>
  <ResponseField
    name="subject"
    type="string"
    required
  >
    <span>{"The subject of the email (e.g. How to build a bot with Botpress!)"}</span>
  </ResponseField>
  <ResponseField
    name="body"
    type="string"
    required
  >
    <span>{"The markdown rich-text body of the email"}</span>
  </ResponseField>
  <ResponseField
    name="replyTo"
    type="string (email)"
  >
    <span>{"The email address of the correspondent (e.g. example@example.com)"}</span>
  </ResponseField>
</Expandable>

**Output**:

This Card has no output.


{/* vale on */}