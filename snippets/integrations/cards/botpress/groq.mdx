{/* This file is auto-generated. Do not edit directly. */}
{/* vale off */}

Here's a reference for all [Cards](/learn/reference/cards/) available with the integration:

### Generate Content

**Input**:

<Expandable title="input fields">
  <ResponseField
    name="model"
    type="object"
  >
    

    <Expandable>
  <ResponseField
    name="id"
    type="enum: openai/gpt-oss-20b, openai/gpt-oss-120b, deepseek-r1-distill-llama-70b, llama-3.3-70b-versatile, llama-3.2-1b-preview, llama-3.2-3b-preview, llama-3.2-11b-vision-preview, llama-3.2-90b-vision-preview, llama-3.1-8b-instant, llama3-8b-8192, llama3-70b-8192, gemma2-9b-it"
    required
  >
    <span>{"Model to use for content generation"}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>
  <ResponseField
    name="reasoningEffort"
    type="enum: low, medium, high, dynamic, none"
  >
    <span>{"Reasoning effort level to use for models that support reasoning. Specifying \"none\" will indicate the LLM to not use reasoning (for models that support optional reasoning). A \"dynamic\" effort will indicate the provider to automatically determine the reasoning effort (if supported by the provider). If not provided the model will not use reasoning for models with optional reasoning or use the default reasoning effort specified by the provider for reasoning-only models.\nNote: A higher reasoning effort will incur in higher output token charges from the LLM provider."}</span>
  </ResponseField>
  <ResponseField
    name="systemPrompt"
    type="string"
  >
    <span>{"Optional system prompt to guide the model"}</span>
  </ResponseField>
  <ResponseField
    name="messages"
    type="array"
    required
  >
    <span>{"Array of messages for the model to process"}</span>

    <Expandable title="array item properties">
  <ResponseField
    name="role"
    type="enum: user, assistant"
    required
  >
    
  </ResponseField>
  <ResponseField
    name="type"
    type="enum: text, tool_calls, tool_result, multipart"
  >
    
  </ResponseField>
  <ResponseField
    name="toolCalls"
    type="array"
  >
    <span>{"Required if `type` is \"tool_calls\""}</span>

    <Expandable title="array item properties">
  <ResponseField
    name="id"
    type="string"
    required
  >
    
  </ResponseField>
  <ResponseField
    name="type"
    type="string"
    required
  >
    
  </ResponseField>
  <ResponseField
    name="function"
    type="object"
    required
  >
    

    <Expandable>
  <ResponseField
    name="name"
    type="string"
    required
  >
    
  </ResponseField>
  <ResponseField
    name="arguments"
    type="string"
    required
  >
    
  </ResponseField>
    </Expandable>
  </ResponseField>
    </Expandable>
  </ResponseField>
  <ResponseField
    name="toolResultCallId"
    type="string"
  >
    <span>{"Required if `type` is \"tool_result\""}</span>
  </ResponseField>
  <ResponseField
    name="content"
    type="string"
    required
  >
    
  </ResponseField>
    </Expandable>
  </ResponseField>
  <ResponseField
    name="responseFormat"
    type="enum: text, json_object"
  >
    <span>{"Response format expected from the model. If \"json_object\" is chosen, you must instruct the model to generate JSON either via the system prompt or a user message."}</span>
  </ResponseField>
  <ResponseField
    name="maxTokens"
    type="number"
  >
    <span>{"Maximum number of tokens allowed in the generated response"}</span>
  </ResponseField>
  <ResponseField
    name="temperature"
    type="number"
  >
    <span>{"Sampling temperature for the model. Higher values result in more random outputs."}</span>
  </ResponseField>
  <ResponseField
    name="topP"
    type="number"
  >
    <span>{"Top-p sampling parameter. Limits sampling to the smallest set of tokens with a cumulative probability above the threshold."}</span>
  </ResponseField>
  <ResponseField
    name="stopSequences"
    type="array"
  >
    <span>{"Sequences where the model should stop generating further tokens."}</span>
  </ResponseField>
  <ResponseField
    name="tools"
    type="array"
  >
    

    <Expandable title="array item properties">
  <ResponseField
    name="type"
    type="string"
    required
  >
    
  </ResponseField>
  <ResponseField
    name="function"
    type="object"
    required
  >
    

    <Expandable>
  <ResponseField
    name="name"
    type="string"
    required
  >
    <span>{"Function name"}</span>
  </ResponseField>
  <ResponseField
    name="description"
    type="string"
  >
    
  </ResponseField>
  <ResponseField
    name="argumentsSchema"
    type="object"
  >
    <span>{"JSON schema of the function arguments"}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>
    </Expandable>
  </ResponseField>
  <ResponseField
    name="toolChoice"
    type="object"
  >
    

    <Expandable>
  <ResponseField
    name="type"
    type="enum: auto, specific, any, none, "
  >
    
  </ResponseField>
  <ResponseField
    name="functionName"
    type="string"
  >
    <span>{"Required if `type` is \"specific\""}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>
  <ResponseField
    name="userId"
    type="string"
  >
    
  </ResponseField>
  <ResponseField
    name="debug"
    type="boolean"
  >
    <span>{"Set to `true` to output debug information to the bot logs"}</span>
  </ResponseField>
  <ResponseField
    name="meta"
    type="object"
  >
    

    <Expandable>
  <ResponseField
    name="promptSource"
    type="string"
  >
    <span>{"Source of the prompt, e.g. agent/:id/:version cards/ai-generate, cards/ai-task, nodes/autonomous, etc."}</span>
  </ResponseField>
  <ResponseField
    name="promptCategory"
    type="string"
  >
    
  </ResponseField>
  <ResponseField
    name="integrationName"
    type="string"
  >
    <span>{"Name of the integration that originally received the message that initiated this action"}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>
</Expandable>

**Output**:

<Expandable title="output">
  <ResponseField
    name="id"
    type="string"
    required
  >
    <span>{"Response ID from LLM provider"}</span>
  </ResponseField>
  <ResponseField
    name="provider"
    type="string"
    required
  >
    <span>{"LLM provider name"}</span>
  </ResponseField>
  <ResponseField
    name="model"
    type="string"
    required
  >
    <span>{"Model name"}</span>
  </ResponseField>
  <ResponseField
    name="choices"
    type="array"
    required
  >
    

    <Expandable title="array item properties">
  <ResponseField
    name="type"
    type="enum: text, tool_calls, tool_result, multipart"
  >
    
  </ResponseField>
  <ResponseField
    name="toolCalls"
    type="array"
  >
    <span>{"Required if `type` is \"tool_calls\""}</span>

    <Expandable title="array item properties">
  <ResponseField
    name="id"
    type="string"
    required
  >
    
  </ResponseField>
  <ResponseField
    name="type"
    type="string"
    required
  >
    
  </ResponseField>
  <ResponseField
    name="function"
    type="object"
    required
  >
    

    <Expandable>
  <ResponseField
    name="name"
    type="string"
    required
  >
    
  </ResponseField>
  <ResponseField
    name="arguments"
    type="string"
    required
  >
    
  </ResponseField>
    </Expandable>
  </ResponseField>
    </Expandable>
  </ResponseField>
  <ResponseField
    name="toolResultCallId"
    type="string"
  >
    <span>{"Required if `type` is \"tool_result\""}</span>
  </ResponseField>
  <ResponseField
    name="content"
    type="string"
    required
  >
    
  </ResponseField>
  <ResponseField
    name="role"
    type="string"
    required
  >
    
  </ResponseField>
  <ResponseField
    name="index"
    type="integer"
    required
  >
    
  </ResponseField>
  <ResponseField
    name="stopReason"
    type="enum: stop, max_tokens, tool_calls, content_filter, other"
    required
  >
    
  </ResponseField>
    </Expandable>
  </ResponseField>
  <ResponseField
    name="usage"
    type="object"
    required
  >
    

    <Expandable>
  <ResponseField
    name="inputTokens"
    type="integer"
    required
  >
    <span>{"Number of input tokens used by the model"}</span>
  </ResponseField>
  <ResponseField
    name="inputCost"
    type="number"
    required
  >
    <span>{"Cost of the input tokens received by the model, in U.S. dollars"}</span>
  </ResponseField>
  <ResponseField
    name="outputTokens"
    type="integer"
    required
  >
    <span>{"Number of output tokens used by the model"}</span>
  </ResponseField>
  <ResponseField
    name="outputCost"
    type="number"
    required
  >
    <span>{"Cost of the output tokens generated by the model, in U.S. dollars"}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>
  <ResponseField
    name="botpress"
    type="object"
    required
  >
    

    <Expandable>
  <ResponseField
    name="cost"
    type="number"
    required
  >
    <span>{"Total cost of the content generation, in U.S. dollars"}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>
</Expandable>

### List Language Models

**Input**:

This Card has no input fields.

**Output**:

<Expandable title="output">
  <ResponseField
    name="models"
    type="array"
    required
  >
    
  </ResponseField>
</Expandable>

### List Speech To Text Models

**Input**:

This Card has no input fields.

**Output**:

<Expandable title="output">
  <ResponseField
    name="models"
    type="array"
    required
  >
    
  </ResponseField>
</Expandable>

### Transcribe Audio

**Input**:

<Expandable title="input fields">
  <ResponseField
    name="model"
    type="object"
  >
    

    <Expandable>
  <ResponseField
    name="id"
    type="string"
    required
  >
    
  </ResponseField>
    </Expandable>
  </ResponseField>
  <ResponseField
    name="fileUrl"
    type="string (uri)"
    required
  >
    <span>{"URL of the audio file to transcribe. The URL should return a content-type header in order to detect the audio format. Supported audio formats supported are: mp3, mp4, mpeg, mpga, m4a, wav, webm"}</span>
  </ResponseField>
  <ResponseField
    name="language"
    type="string"
  >
    <span>{"The language of the input audio (optional). Supplying the input language in ISO-639-1 format will improve accuracy and latency."}</span>
  </ResponseField>
  <ResponseField
    name="prompt"
    type="string"
  >
    <span>{"An optional text to guide the model's style or continue a previous audio segment. The prompt should match the audio language."}</span>
  </ResponseField>
  <ResponseField
    name="temperature"
    type="number"
  >
    <span>{"The sampling temperature (optional), between 0 and 1. Defaults to 0 (automatic). Higher values like 0.8 will make the output more random, while lower values like 0.2 will make it more focused and deterministic."}</span>
  </ResponseField>
</Expandable>

**Output**:

<Expandable title="output">
  <ResponseField
    name="language"
    type="string"
    required
  >
    <span>{"Detected language of the audio"}</span>
  </ResponseField>
  <ResponseField
    name="duration"
    type="number"
    required
  >
    <span>{"Duration of the audio file, in seconds"}</span>
  </ResponseField>
  <ResponseField
    name="segments"
    type="array"
    required
  >
    

    <Expandable title="array item properties">
  <ResponseField
    name="text"
    type="string"
    required
  >
    <span>{"Text content of the segment."}</span>
  </ResponseField>
  <ResponseField
    name="id"
    type="number"
    required
  >
    <span>{"Unique identifier of the segment"}</span>
  </ResponseField>
  <ResponseField
    name="seek"
    type="number"
    required
  >
    <span>{"Seek offset of the segment"}</span>
  </ResponseField>
  <ResponseField
    name="start"
    type="number"
    required
  >
    <span>{"Start time of the segment in seconds."}</span>
  </ResponseField>
  <ResponseField
    name="end"
    type="number"
    required
  >
    <span>{"End time of the segment in seconds."}</span>
  </ResponseField>
  <ResponseField
    name="tokens"
    type="array"
    required
  >
    <span>{"Array of token IDs for the text content."}</span>
  </ResponseField>
  <ResponseField
    name="temperature"
    type="number"
    required
  >
    <span>{"Temperature parameter used for generating the segment."}</span>
  </ResponseField>
  <ResponseField
    name="avg_logprob"
    type="number"
    required
  >
    <span>{"Average logprob of the segment. If the value is lower than -1, consider the logprobs failed."}</span>
  </ResponseField>
  <ResponseField
    name="compression_ratio"
    type="number"
    required
  >
    <span>{"Compression ratio of the segment. If the value is greater than 2.4, consider the compression failed."}</span>
  </ResponseField>
  <ResponseField
    name="no_speech_prob"
    type="number"
    required
  >
    <span>{"Probability of no speech in the segment. If the value is higher than 1.0 and the avg_logprob is below -1, consider this segment silent."}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>
  <ResponseField
    name="model"
    type="string"
    required
  >
    <span>{"Model name used"}</span>
  </ResponseField>
  <ResponseField
    name="cost"
    type="number"
    required
  >
    <span>{"Total cost of the transcription, in U.S. dollars (DEPRECATED)"}</span>
  </ResponseField>
  <ResponseField
    name="botpress"
    type="object"
    required
  >
    

    <Expandable>
  <ResponseField
    name="cost"
    type="number"
    required
  >
    <span>{"Total cost of the transcription, in U.S. dollars"}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>
</Expandable>


{/* vale on */}