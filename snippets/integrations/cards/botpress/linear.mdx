{/* This file is auto-generated. Do not edit directly. */}
{/* vale off */}

Here's a reference for all [Cards](/learn/reference/cards/) available with the integration:

### Create Issue

<span>{"Create an issue on Linear"}</span>

**Input**:

<Expandable title="input fields">
  <ResponseField
    name="title"
    type="string"
    required
  >
    <span>{"The issue's title"}</span>
  </ResponseField>
  <ResponseField
    name="description"
    type="string"
    required
  >
    <span>{"The content of the issue"}</span>
  </ResponseField>
  <ResponseField
    name="priority"
    type="number"
  >
    <span>{"0 = none, 1 = urgent, 2 = high, 3 = medium, 4 = low"}</span>
  </ResponseField>
  <ResponseField
    name="teamName"
    type="string"
    required
  >
    <span>{"Name of the team to assign the issue to"}</span>
  </ResponseField>
  <ResponseField
    name="labels"
    type="array"
  >
    <span>{"One or multiple labels to assign to this issue"}</span>
  </ResponseField>
  <ResponseField
    name="project"
    type="string"
  >
    <span>{"A project to associate to this issue"}</span>
  </ResponseField>
</Expandable>

**Output**:

<Expandable title="output">
  <ResponseField
    name="issue"
    type="object"
    required
  >
    <span>{"The created issue"}</span>

    <Expandable>
  <ResponseField
    name="title"
    type="string"
    required
  >
    <span>{"The issue title on Linear, such as \"Fix the bug"}</span>
  </ResponseField>
  <ResponseField
    name="number"
    type="number"
    required
  >
    <span>{"The issue number on Linear, such as \"123\" in XXX-123"}</span>
  </ResponseField>
  <ResponseField
    name="createdAt"
    type="string (date-time)"
    required
  >
    <span>{"The ISO date the issue was created"}</span>
  </ResponseField>
  <ResponseField
    name="updatedAt"
    type="string (date-time)"
    required
  >
    <span>{"The ISO date the issue was last updated"}</span>
  </ResponseField>
  <ResponseField
    name="priority"
    type="number"
    required
  >
    <span>{"Priority of the issue, such as \"1\" for \"Urgent\", 0 for \"No Priority\""}</span>
  </ResponseField>
  <ResponseField
    name="description"
    type="string"
  >
    <span>{"A markdown description of the issue. Images and videos are inlined using markdown links."}</span>
  </ResponseField>
  <ResponseField
    name="id"
    type="string"
    required
  >
    <span>{"The issue ID on Linear"}</span>
  </ResponseField>
  <ResponseField
    name="identifier"
    type="string"
    required
  >
    <span>{"Issue's human readable identifier (e.g. XXX-123)"}</span>
  </ResponseField>
  <ResponseField
    name="estimate"
    type="number"
  >
    <span>{"The estimate of the issue in points"}</span>
  </ResponseField>
  <ResponseField
    name="url"
    type="string"
    required
  >
    <span>{"The URL of the issue on Linear"}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>
</Expandable>

### Delete Issue

<span>{"Delete an issue on Linear"}</span>

**Input**:

<Expandable title="input fields">
  <ResponseField
    name="id"
    type="string"
    required
  >
    <span>{"The issue ID on Linear. Ex: {{event.payload.linearIds.issueId}}"}</span>
  </ResponseField>
</Expandable>

**Output**:

This Card has no output.

### Find Target

<span>{"Find a target on Linear"}</span>

**Input**:

<Expandable title="input fields">
  <ResponseField
    name="query"
    type="string"
    required
  >
    <span>{"Issue title, description, or number"}</span>
  </ResponseField>
  <ResponseField
    name="channel"
    type="enum: issue"
    required
  >
    <span>{"The channel to search in"}</span>
  </ResponseField>
</Expandable>

**Output**:

<Expandable title="output">
  <ResponseField
    name="targets"
    type="array"
    required
  >
    <span>{"The list of issues found"}</span>

    <Expandable title="array item properties">
  <ResponseField
    name="displayName"
    type="string"
    required
  >
    <span>{"The name of the issue"}</span>
  </ResponseField>
  <ResponseField
    name="tags"
    type="object"
    required
  >
    <span>{"The tags applied to the issue"}</span>
  </ResponseField>
  <ResponseField
    name="channel"
    type="enum: issue"
    required
  >
    <span>{"The channel of the search result"}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>
</Expandable>

### Get Issue

<span>{"Get an issue by its ID"}</span>

**Input**:

<Expandable title="input fields">
  <ResponseField
    name="issueId"
    type="string"
    required
  >
    <span>{"The issue ID on Linear. Ex: {{event.payload.linearIds.issueId}}"}</span>
  </ResponseField>
</Expandable>

**Output**:

<Expandable title="output">
  <ResponseField
    name="title"
    type="string"
    required
  >
    <span>{"The issue title on Linear, such as \"Fix the bug"}</span>
  </ResponseField>
  <ResponseField
    name="number"
    type="number"
    required
  >
    <span>{"The issue number on Linear, such as \"123\" in XXX-123"}</span>
  </ResponseField>
  <ResponseField
    name="createdAt"
    type="string (date-time)"
    required
  >
    <span>{"The ISO date the issue was created"}</span>
  </ResponseField>
  <ResponseField
    name="updatedAt"
    type="string (date-time)"
    required
  >
    <span>{"The ISO date the issue was last updated"}</span>
  </ResponseField>
  <ResponseField
    name="priority"
    type="number"
    required
  >
    <span>{"Priority of the issue, such as \"1\" for \"Urgent\", 0 for \"No Priority\""}</span>
  </ResponseField>
  <ResponseField
    name="description"
    type="string"
  >
    <span>{"A markdown description of the issue. Images and videos are inlined using markdown links."}</span>
  </ResponseField>
  <ResponseField
    name="id"
    type="string"
    required
  >
    <span>{"The issue ID on Linear"}</span>
  </ResponseField>
  <ResponseField
    name="identifier"
    type="string"
    required
  >
    <span>{"Issue's human readable identifier (e.g. XXX-123)"}</span>
  </ResponseField>
  <ResponseField
    name="estimate"
    type="number"
  >
    <span>{"The estimate of the issue in points"}</span>
  </ResponseField>
  <ResponseField
    name="url"
    type="string"
    required
  >
    <span>{"The URL of the issue on Linear"}</span>
  </ResponseField>
</Expandable>

### Get User Profile

<span>{"Get a user profile from Linear"}</span>

**Input**:

<Expandable title="input fields">
  <ResponseField
    name="linearUserId"
    type="string"
    required
  >
    <span>{"The user's ID on Linear. Ex: {{event.payload.linearIds.creatorId}}"}</span>
  </ResponseField>
</Expandable>

**Output**:

<Expandable title="output">
  <ResponseField
    name="linearId"
    type="string"
    required
  >
    <span>{"Linear User ID"}</span>
  </ResponseField>
  <ResponseField
    name="admin"
    type="boolean"
    required
  >
    <span>{"Indicates if the user is an admin of the organization"}</span>
  </ResponseField>
  <ResponseField
    name="archivedAt"
    type="string (date-time)"
  >
    <span>{"Date when the user was archived"}</span>
  </ResponseField>
  <ResponseField
    name="avatarUrl"
    type="string (uri)"
  >
    <span>{"The URL for the profile picture"}</span>
  </ResponseField>
  <ResponseField
    name="createdAt"
    type="string (date-time)"
    required
  >
    <span>{"Date when the user was created"}</span>
  </ResponseField>
  <ResponseField
    name="description"
    type="string"
  >
    <span>{"A short description of the user, either its title or bio"}</span>
  </ResponseField>
  <ResponseField
    name="displayName"
    type="string"
    required
  >
    <span>{"The user's display (nick) name. Unique within each organization"}</span>
  </ResponseField>
  <ResponseField
    name="guest"
    type="boolean"
    required
  >
    <span>{"Whether the user is a guest in the workspace and limited to accessing a subset of teams"}</span>
  </ResponseField>
  <ResponseField
    name="email"
    type="string"
    required
  >
    <span>{"The user's email address"}</span>
  </ResponseField>
  <ResponseField
    name="isMe"
    type="boolean"
    required
  >
    <span>{"Whether the user is the currently authenticated user"}</span>
  </ResponseField>
  <ResponseField
    name="url"
    type="string"
    required
  >
    <span>{"User's profile URL"}</span>
  </ResponseField>
  <ResponseField
    name="timezone"
    type="string"
  >
    <span>{"The local timezone of the user"}</span>
  </ResponseField>
  <ResponseField
    name="name"
    type="string"
    required
  >
    <span>{"The user's full name"}</span>
  </ResponseField>
</Expandable>

### Issue Delete

**Input**:

<Expandable title="input fields">
  <ResponseField
    name="id"
    type="string"
    required
  >
    
  </ResponseField>
</Expandable>

**Output**:

This Card has no output.

### Issue List

**Input**:

<Expandable title="input fields">
  <ResponseField
    name="nextToken"
    type="string"
  >
    
  </ResponseField>
</Expandable>

**Output**:

<Expandable title="output">
  <ResponseField
    name="items"
    type="array"
    required
  >
    
  </ResponseField>
  <ResponseField
    name="meta"
    type="object"
    required
  >
    

    <Expandable>
  <ResponseField
    name="nextToken"
    type="string"
  >
    
  </ResponseField>
    </Expandable>
  </ResponseField>
</Expandable>

### List Issues

<span>{"List issues from Linear"}</span>

**Input**:

<Expandable title="input fields">
  <ResponseField
    name="count"
    type="number"
  >
    <span>{"The number of issues to return"}</span>
  </ResponseField>
  <ResponseField
    name="startCursor"
    type="string"
  >
    <span>{"The cursor to start from"}</span>
  </ResponseField>
  <ResponseField
    name="teamId"
    type="string"
  >
    <span>{"The team ID to filter by"}</span>
  </ResponseField>
  <ResponseField
    name="startDate"
    type="string"
  >
    <span>{"Ignore issues created before this date"}</span>
  </ResponseField>
</Expandable>

**Output**:

<Expandable title="output">
  <ResponseField
    name="issues"
    type="array"
    required
  >
    <span>{"The list of matching issues"}</span>

    <Expandable title="array item properties">
  <ResponseField
    name="title"
    type="string"
    required
  >
    <span>{"The issue title on Linear, such as \"Fix the bug"}</span>
  </ResponseField>
  <ResponseField
    name="number"
    type="number"
    required
  >
    <span>{"The issue number on Linear, such as \"123\" in XXX-123"}</span>
  </ResponseField>
  <ResponseField
    name="createdAt"
    type="string (date-time)"
    required
  >
    <span>{"The ISO date the issue was created"}</span>
  </ResponseField>
  <ResponseField
    name="updatedAt"
    type="string (date-time)"
    required
  >
    <span>{"The ISO date the issue was last updated"}</span>
  </ResponseField>
  <ResponseField
    name="priority"
    type="number"
    required
  >
    <span>{"Priority of the issue, such as \"1\" for \"Urgent\", 0 for \"No Priority\""}</span>
  </ResponseField>
  <ResponseField
    name="description"
    type="string"
  >
    <span>{"A markdown description of the issue. Images and videos are inlined using markdown links."}</span>
  </ResponseField>
  <ResponseField
    name="id"
    type="string"
    required
  >
    <span>{"The issue ID on Linear"}</span>
  </ResponseField>
  <ResponseField
    name="identifier"
    type="string"
    required
  >
    <span>{"Issue's human readable identifier (e.g. XXX-123)"}</span>
  </ResponseField>
  <ResponseField
    name="estimate"
    type="number"
  >
    <span>{"The estimate of the issue in points"}</span>
  </ResponseField>
  <ResponseField
    name="url"
    type="string"
    required
  >
    <span>{"The URL of the issue on Linear"}</span>
  </ResponseField>
  <ResponseField
    name="linearIds"
    type="object"
  >
    <span>{"The Linear IDs of the referenced entities"}</span>

    <Expandable>
  <ResponseField
    name="creatorId"
    type="string"
  >
    <span>{"The internal Linear User ID of the user who created the issue"}</span>
  </ResponseField>
  <ResponseField
    name="labelIds"
    type="array"
  >
    <span>{"The internal Linear Label IDs associated with the issue"}</span>
  </ResponseField>
  <ResponseField
    name="issueId"
    type="string"
    required
  >
    <span>{"The internal Linear Issue ID"}</span>
  </ResponseField>
  <ResponseField
    name="teamId"
    type="string"
  >
    <span>{"The internal Linear Team ID"}</span>
  </ResponseField>
  <ResponseField
    name="projectId"
    type="string"
  >
    <span>{"The internal Linear Project ID"}</span>
  </ResponseField>
  <ResponseField
    name="assigneeId"
    type="string"
  >
    <span>{"The internal Linear Assignee ID"}</span>
  </ResponseField>
  <ResponseField
    name="subscriberIds"
    type="array"
  >
    <span>{"The internal Linear Subscriber User IDs"}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>
    </Expandable>
  </ResponseField>
  <ResponseField
    name="nextCursor"
    type="string"
  >
    <span>{"The cursor to fetch the next page"}</span>
  </ResponseField>
</Expandable>

### List Teams

<span>{"List teams from Linear"}</span>

**Input**:

This Card has no input fields.

**Output**:

<Expandable title="output">
  <ResponseField
    name="teams"
    type="array"
    required
  >
    <span>{"The list of teams"}</span>

    <Expandable title="array item properties">
  <ResponseField
    name="id"
    type="string"
    required
  >
    <span>{"The unique identifier of the entity"}</span>
  </ResponseField>
  <ResponseField
    name="name"
    type="string"
    required
  >
    <span>{"The team's name"}</span>
  </ResponseField>
  <ResponseField
    name="description"
    type="string"
  >
    <span>{"The team's description"}</span>
  </ResponseField>
  <ResponseField
    name="icon"
    type="string"
  >
    <span>{"The icon of the team"}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>
</Expandable>

### List Users

<span>{"List users from Linear"}</span>

**Input**:

<Expandable title="input fields">
  <ResponseField
    name="count"
    type="number"
  >
    <span>{"The number of users to return"}</span>
  </ResponseField>
  <ResponseField
    name="startCursor"
    type="string"
  >
    <span>{"The cursor to start from"}</span>
  </ResponseField>
</Expandable>

**Output**:

<Expandable title="output">
  <ResponseField
    name="users"
    type="array"
    required
  >
    <span>{"The list of matching users"}</span>

    <Expandable title="array item properties">
  <ResponseField
    name="id"
    type="string"
    required
  >
    <span>{"The user ID on Linear"}</span>
  </ResponseField>
  <ResponseField
    name="email"
    type="string"
    required
  >
    <span>{"The user's email address"}</span>
  </ResponseField>
  <ResponseField
    name="name"
    type="string"
    required
  >
    <span>{"The user's nickname"}</span>
  </ResponseField>
  <ResponseField
    name="displayName"
    type="string"
    required
  >
    <span>{"The user's display name"}</span>
  </ResponseField>
  <ResponseField
    name="avatarUrl"
    type="string"
  >
    <span>{"The URL of the user avatar"}</span>
  </ResponseField>
  <ResponseField
    name="description"
    type="string"
  >
    <span>{"The user's description, such as their title or bio"}</span>
  </ResponseField>
  <ResponseField
    name="lastSeen"
    type="string"
  >
    <span>{"The last time the user was seen"}</span>
  </ResponseField>
  <ResponseField
    name="updatedAt"
    type="string (date-time)"
    required
  >
    <span>{"The last time the user was updated"}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>
  <ResponseField
    name="nextCursor"
    type="string"
  >
    <span>{"The cursor to fetch the next page"}</span>
  </ResponseField>
</Expandable>

### Mark Issue as Duplicate

<span>{"Mark an issue as a duplicate of another"}</span>

**Input**:

<Expandable title="input fields">
  <ResponseField
    name="issueId"
    type="string"
    required
  >
    <span>{"The issue ID on Linear. Ex: {{event.payload.linearIds.issueId}}"}</span>
  </ResponseField>
  <ResponseField
    name="relatedIssueId"
    type="string"
    required
  >
    <span>{"The ID of the existing issue on Linear"}</span>
  </ResponseField>
</Expandable>

**Output**:

This Card has no output.

### Update Issue

<span>{"Update an issue on Linear"}</span>

**Input**:

<Expandable title="input fields">
  <ResponseField
    name="issueId"
    type="string"
    required
  >
    <span>{"The issue ID on Linear. Example: {{event.payload.id}}"}</span>
  </ResponseField>
  <ResponseField
    name="priority"
    type="number"
  >
    <span>{"0 = none, 1 = urgent, 2 = high, 3 = medium, 4 = low"}</span>
  </ResponseField>
  <ResponseField
    name="teamName"
    type="string"
  >
    <span>{"Type a name to change the assigned team of the issue"}</span>
  </ResponseField>
  <ResponseField
    name="labels"
    type="array"
  >
    <span>{"One or multiple labels to assign to this issue"}</span>
  </ResponseField>
  <ResponseField
    name="project"
    type="string"
  >
    <span>{"A project to associate to this issue"}</span>
  </ResponseField>
</Expandable>

**Output**:

<Expandable title="output">
  <ResponseField
    name="issue"
    type="object"
  >
    <span>{"The updated issue"}</span>

    <Expandable>
  <ResponseField
    name="title"
    type="string"
    required
  >
    <span>{"The issue title on Linear, such as \"Fix the bug"}</span>
  </ResponseField>
  <ResponseField
    name="number"
    type="number"
    required
  >
    <span>{"The issue number on Linear, such as \"123\" in XXX-123"}</span>
  </ResponseField>
  <ResponseField
    name="createdAt"
    type="string (date-time)"
    required
  >
    <span>{"The ISO date the issue was created"}</span>
  </ResponseField>
  <ResponseField
    name="updatedAt"
    type="string (date-time)"
    required
  >
    <span>{"The ISO date the issue was last updated"}</span>
  </ResponseField>
  <ResponseField
    name="priority"
    type="number"
    required
  >
    <span>{"Priority of the issue, such as \"1\" for \"Urgent\", 0 for \"No Priority\""}</span>
  </ResponseField>
  <ResponseField
    name="description"
    type="string"
  >
    <span>{"A markdown description of the issue. Images and videos are inlined using markdown links."}</span>
  </ResponseField>
  <ResponseField
    name="id"
    type="string"
    required
  >
    <span>{"The issue ID on Linear"}</span>
  </ResponseField>
  <ResponseField
    name="identifier"
    type="string"
    required
  >
    <span>{"Issue's human readable identifier (e.g. XXX-123)"}</span>
  </ResponseField>
  <ResponseField
    name="estimate"
    type="number"
  >
    <span>{"The estimate of the issue in points"}</span>
  </ResponseField>
  <ResponseField
    name="url"
    type="string"
    required
  >
    <span>{"The URL of the issue on Linear"}</span>
  </ResponseField>
    </Expandable>
  </ResponseField>
</Expandable>


{/* vale on */}