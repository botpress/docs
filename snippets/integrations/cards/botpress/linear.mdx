{/* This file is auto-generated. Do not edit directly. */}
{/* vale off */}

Here's a reference for all [Cards](/learn/reference/cards/) available with the integration:

### Create Issue

Create an issue on Linear

**Input**:

<Expandable title="input fields">
  <ResponseField
    name="title"
    type="string"
    required
  >
    The issue's title
  </ResponseField>
  <ResponseField
    name="description"
    type="string"
    required
  >
    The content of the issue
  </ResponseField>
  <ResponseField
    name="priority"
    type="number"
  >
    0 = none, 1 = urgent, 2 = high, 3 = medium, 4 = low
  </ResponseField>
  <ResponseField
    name="teamName"
    type="string"
    required
  >
    Name of the team to assign the issue to
  </ResponseField>
  <ResponseField
    name="labels"
    type="array"
  >
    One or multiple labels to assign to this issue
  </ResponseField>
  <ResponseField
    name="project"
    type="string"
  >
    A project to associate to this issue
  </ResponseField>
</Expandable>

**Output**:

<Expandable title="output">
  <ResponseField
    name="issue"
    type="object"
    required
  >
    The created issue

    <Expandable>
  <ResponseField
    name="title"
    type="string"
    required
  >
    The issue title on Linear, such as "Fix the bug
  </ResponseField>
  <ResponseField
    name="number"
    type="number"
    required
  >
    The issue number on Linear, such as "123" in XXX-123
  </ResponseField>
  <ResponseField
    name="createdAt"
    type="string (date-time)"
    required
  >
    The ISO date the issue was created
  </ResponseField>
  <ResponseField
    name="updatedAt"
    type="string (date-time)"
    required
  >
    The ISO date the issue was last updated
  </ResponseField>
  <ResponseField
    name="priority"
    type="number"
    required
  >
    Priority of the issue, such as "1" for "Urgent", 0 for "No Priority"
  </ResponseField>
  <ResponseField
    name="description"
    type="string"
  >
    A markdown description of the issue. Images and videos are inlined using markdown links.
  </ResponseField>
  <ResponseField
    name="id"
    type="string"
    required
  >
    The issue ID on Linear
  </ResponseField>
  <ResponseField
    name="identifier"
    type="string"
    required
  >
    Issue's human readable identifier (e.g. XXX-123)
  </ResponseField>
  <ResponseField
    name="estimate"
    type="number"
  >
    The estimate of the issue in points
  </ResponseField>
  <ResponseField
    name="url"
    type="string"
    required
  >
    The URL of the issue on Linear
  </ResponseField>
    </Expandable>
  </ResponseField>
</Expandable>

### Delete Issue

Delete an issue on Linear

**Input**:

<Expandable title="input fields">
  <ResponseField
    name="id"
    type="string"
    required
  >
    The issue ID on Linear. Ex: {{event.payload.linearIds.issueId}}
  </ResponseField>
</Expandable>

**Output**:

This Card has no output.

### Find Target

Find a target on Linear

**Input**:

<Expandable title="input fields">
  <ResponseField
    name="query"
    type="string"
    required
  >
    Issue title, description, or number
  </ResponseField>
  <ResponseField
    name="channel"
    type="enum: issue"
    required
  >
    The channel to search in
  </ResponseField>
</Expandable>

**Output**:

<Expandable title="output">
  <ResponseField
    name="targets"
    type="array"
    required
  >
    The list of issues found

    <Expandable title="array item properties">
  <ResponseField
    name="displayName"
    type="string"
    required
  >
    The name of the issue
  </ResponseField>
  <ResponseField
    name="tags"
    type="object"
    required
  >
    The tags applied to the issue
  </ResponseField>
  <ResponseField
    name="channel"
    type="enum: issue"
    required
  >
    The channel of the search result
  </ResponseField>
    </Expandable>
  </ResponseField>
</Expandable>

### Get Issue

Get an issue by its ID

**Input**:

<Expandable title="input fields">
  <ResponseField
    name="issueId"
    type="string"
    required
  >
    The issue ID on Linear. Ex: {{event.payload.linearIds.issueId}}
  </ResponseField>
</Expandable>

**Output**:

<Expandable title="output">
  <ResponseField
    name="title"
    type="string"
    required
  >
    The issue title on Linear, such as "Fix the bug
  </ResponseField>
  <ResponseField
    name="number"
    type="number"
    required
  >
    The issue number on Linear, such as "123" in XXX-123
  </ResponseField>
  <ResponseField
    name="createdAt"
    type="string (date-time)"
    required
  >
    The ISO date the issue was created
  </ResponseField>
  <ResponseField
    name="updatedAt"
    type="string (date-time)"
    required
  >
    The ISO date the issue was last updated
  </ResponseField>
  <ResponseField
    name="priority"
    type="number"
    required
  >
    Priority of the issue, such as "1" for "Urgent", 0 for "No Priority"
  </ResponseField>
  <ResponseField
    name="description"
    type="string"
  >
    A markdown description of the issue. Images and videos are inlined using markdown links.
  </ResponseField>
  <ResponseField
    name="id"
    type="string"
    required
  >
    The issue ID on Linear
  </ResponseField>
  <ResponseField
    name="identifier"
    type="string"
    required
  >
    Issue's human readable identifier (e.g. XXX-123)
  </ResponseField>
  <ResponseField
    name="estimate"
    type="number"
  >
    The estimate of the issue in points
  </ResponseField>
  <ResponseField
    name="url"
    type="string"
    required
  >
    The URL of the issue on Linear
  </ResponseField>
</Expandable>

### Get User Profile

Get a user profile from Linear

**Input**:

<Expandable title="input fields">
  <ResponseField
    name="linearUserId"
    type="string"
    required
  >
    The user's ID on Linear. Ex: {{event.payload.linearIds.creatorId}}
  </ResponseField>
</Expandable>

**Output**:

<Expandable title="output">
  <ResponseField
    name="linearId"
    type="string"
    required
  >
    Linear User ID
  </ResponseField>
  <ResponseField
    name="admin"
    type="boolean"
    required
  >
    Indicates if the user is an admin of the organization
  </ResponseField>
  <ResponseField
    name="archivedAt"
    type="string (date-time)"
  >
    Date when the user was archived
  </ResponseField>
  <ResponseField
    name="avatarUrl"
    type="string (uri)"
  >
    The URL for the profile picture
  </ResponseField>
  <ResponseField
    name="createdAt"
    type="string (date-time)"
    required
  >
    Date when the user was created
  </ResponseField>
  <ResponseField
    name="description"
    type="string"
  >
    A short description of the user, either its title or bio
  </ResponseField>
  <ResponseField
    name="displayName"
    type="string"
    required
  >
    The user's display (nick) name. Unique within each organization
  </ResponseField>
  <ResponseField
    name="guest"
    type="boolean"
    required
  >
    Whether the user is a guest in the workspace and limited to accessing a subset of teams
  </ResponseField>
  <ResponseField
    name="email"
    type="string"
    required
  >
    The user's email address
  </ResponseField>
  <ResponseField
    name="isMe"
    type="boolean"
    required
  >
    Whether the user is the currently authenticated user
  </ResponseField>
  <ResponseField
    name="url"
    type="string"
    required
  >
    User's profile URL
  </ResponseField>
  <ResponseField
    name="timezone"
    type="string"
  >
    The local timezone of the user
  </ResponseField>
  <ResponseField
    name="name"
    type="string"
    required
  >
    The user's full name
  </ResponseField>
</Expandable>

### Issue Delete

**Input**:

<Expandable title="input fields">
  <ResponseField
    name="id"
    type="string"
    required
  >
    
  </ResponseField>
</Expandable>

**Output**:

This Card has no output.

### Issue List

**Input**:

<Expandable title="input fields">
  <ResponseField
    name="nextToken"
    type="string"
  >
    
  </ResponseField>
</Expandable>

**Output**:

<Expandable title="output">
  <ResponseField
    name="items"
    type="array"
    required
  >
    
  </ResponseField>
  <ResponseField
    name="meta"
    type="object"
    required
  >
    

    <Expandable>
  <ResponseField
    name="nextToken"
    type="string"
  >
    
  </ResponseField>
    </Expandable>
  </ResponseField>
</Expandable>

### List Issues

List issues from Linear

**Input**:

<Expandable title="input fields">
  <ResponseField
    name="count"
    type="number"
  >
    The number of issues to return
  </ResponseField>
  <ResponseField
    name="startCursor"
    type="string"
  >
    The cursor to start from
  </ResponseField>
  <ResponseField
    name="teamId"
    type="string"
  >
    The team ID to filter by
  </ResponseField>
  <ResponseField
    name="startDate"
    type="string"
  >
    Ignore issues created before this date
  </ResponseField>
</Expandable>

**Output**:

<Expandable title="output">
  <ResponseField
    name="issues"
    type="array"
    required
  >
    The list of matching issues

    <Expandable title="array item properties">
  <ResponseField
    name="title"
    type="string"
    required
  >
    The issue title on Linear, such as "Fix the bug
  </ResponseField>
  <ResponseField
    name="number"
    type="number"
    required
  >
    The issue number on Linear, such as "123" in XXX-123
  </ResponseField>
  <ResponseField
    name="createdAt"
    type="string (date-time)"
    required
  >
    The ISO date the issue was created
  </ResponseField>
  <ResponseField
    name="updatedAt"
    type="string (date-time)"
    required
  >
    The ISO date the issue was last updated
  </ResponseField>
  <ResponseField
    name="priority"
    type="number"
    required
  >
    Priority of the issue, such as "1" for "Urgent", 0 for "No Priority"
  </ResponseField>
  <ResponseField
    name="description"
    type="string"
  >
    A markdown description of the issue. Images and videos are inlined using markdown links.
  </ResponseField>
  <ResponseField
    name="id"
    type="string"
    required
  >
    The issue ID on Linear
  </ResponseField>
  <ResponseField
    name="identifier"
    type="string"
    required
  >
    Issue's human readable identifier (e.g. XXX-123)
  </ResponseField>
  <ResponseField
    name="estimate"
    type="number"
  >
    The estimate of the issue in points
  </ResponseField>
  <ResponseField
    name="url"
    type="string"
    required
  >
    The URL of the issue on Linear
  </ResponseField>
  <ResponseField
    name="linearIds"
    type="object"
  >
    The Linear IDs of the referenced entities

    <Expandable>
  <ResponseField
    name="creatorId"
    type="string"
  >
    The internal Linear User ID of the user who created the issue
  </ResponseField>
  <ResponseField
    name="labelIds"
    type="array"
  >
    The internal Linear Label IDs associated with the issue
  </ResponseField>
  <ResponseField
    name="issueId"
    type="string"
    required
  >
    The internal Linear Issue ID
  </ResponseField>
  <ResponseField
    name="teamId"
    type="string"
  >
    The internal Linear Team ID
  </ResponseField>
  <ResponseField
    name="projectId"
    type="string"
  >
    The internal Linear Project ID
  </ResponseField>
  <ResponseField
    name="assigneeId"
    type="string"
  >
    The internal Linear Assignee ID
  </ResponseField>
  <ResponseField
    name="subscriberIds"
    type="array"
  >
    The internal Linear Subscriber User IDs
  </ResponseField>
    </Expandable>
  </ResponseField>
    </Expandable>
  </ResponseField>
  <ResponseField
    name="nextCursor"
    type="string"
  >
    The cursor to fetch the next page
  </ResponseField>
</Expandable>

### List Teams

List teams from Linear

**Input**:

This Card has no input fields.

**Output**:

<Expandable title="output">
  <ResponseField
    name="teams"
    type="array"
    required
  >
    The list of teams

    <Expandable title="array item properties">
  <ResponseField
    name="id"
    type="string"
    required
  >
    The unique identifier of the entity
  </ResponseField>
  <ResponseField
    name="name"
    type="string"
    required
  >
    The team's name
  </ResponseField>
  <ResponseField
    name="description"
    type="string"
  >
    The team's description
  </ResponseField>
  <ResponseField
    name="icon"
    type="string"
  >
    The icon of the team
  </ResponseField>
    </Expandable>
  </ResponseField>
</Expandable>

### List Users

List users from Linear

**Input**:

<Expandable title="input fields">
  <ResponseField
    name="count"
    type="number"
  >
    The number of users to return
  </ResponseField>
  <ResponseField
    name="startCursor"
    type="string"
  >
    The cursor to start from
  </ResponseField>
</Expandable>

**Output**:

<Expandable title="output">
  <ResponseField
    name="users"
    type="array"
    required
  >
    The list of matching users

    <Expandable title="array item properties">
  <ResponseField
    name="id"
    type="string"
    required
  >
    The user ID on Linear
  </ResponseField>
  <ResponseField
    name="email"
    type="string"
    required
  >
    The user's email address
  </ResponseField>
  <ResponseField
    name="name"
    type="string"
    required
  >
    The user's nickname
  </ResponseField>
  <ResponseField
    name="displayName"
    type="string"
    required
  >
    The user's display name
  </ResponseField>
  <ResponseField
    name="avatarUrl"
    type="string"
  >
    The URL of the user avatar
  </ResponseField>
  <ResponseField
    name="description"
    type="string"
  >
    The user's description, such as their title or bio
  </ResponseField>
  <ResponseField
    name="lastSeen"
    type="string"
  >
    The last time the user was seen
  </ResponseField>
  <ResponseField
    name="updatedAt"
    type="string (date-time)"
    required
  >
    The last time the user was updated
  </ResponseField>
    </Expandable>
  </ResponseField>
  <ResponseField
    name="nextCursor"
    type="string"
  >
    The cursor to fetch the next page
  </ResponseField>
</Expandable>

### Mark Issue as Duplicate

Mark an issue as a duplicate of another

**Input**:

<Expandable title="input fields">
  <ResponseField
    name="issueId"
    type="string"
    required
  >
    The issue ID on Linear. Ex: {{event.payload.linearIds.issueId}}
  </ResponseField>
  <ResponseField
    name="relatedIssueId"
    type="string"
    required
  >
    The ID of the existing issue on Linear
  </ResponseField>
</Expandable>

**Output**:

This Card has no output.

### Update Issue

Update an issue on Linear

**Input**:

<Expandable title="input fields">
  <ResponseField
    name="issueId"
    type="string"
    required
  >
    The issue ID on Linear. Example: {{event.payload.id}}
  </ResponseField>
  <ResponseField
    name="priority"
    type="number"
  >
    0 = none, 1 = urgent, 2 = high, 3 = medium, 4 = low
  </ResponseField>
  <ResponseField
    name="teamName"
    type="string"
  >
    Type a name to change the assigned team of the issue
  </ResponseField>
  <ResponseField
    name="labels"
    type="array"
  >
    One or multiple labels to assign to this issue
  </ResponseField>
  <ResponseField
    name="project"
    type="string"
  >
    A project to associate to this issue
  </ResponseField>
</Expandable>

**Output**:

<Expandable title="output">
  <ResponseField
    name="issue"
    type="object"
  >
    The updated issue

    <Expandable>
  <ResponseField
    name="title"
    type="string"
    required
  >
    The issue title on Linear, such as "Fix the bug
  </ResponseField>
  <ResponseField
    name="number"
    type="number"
    required
  >
    The issue number on Linear, such as "123" in XXX-123
  </ResponseField>
  <ResponseField
    name="createdAt"
    type="string (date-time)"
    required
  >
    The ISO date the issue was created
  </ResponseField>
  <ResponseField
    name="updatedAt"
    type="string (date-time)"
    required
  >
    The ISO date the issue was last updated
  </ResponseField>
  <ResponseField
    name="priority"
    type="number"
    required
  >
    Priority of the issue, such as "1" for "Urgent", 0 for "No Priority"
  </ResponseField>
  <ResponseField
    name="description"
    type="string"
  >
    A markdown description of the issue. Images and videos are inlined using markdown links.
  </ResponseField>
  <ResponseField
    name="id"
    type="string"
    required
  >
    The issue ID on Linear
  </ResponseField>
  <ResponseField
    name="identifier"
    type="string"
    required
  >
    Issue's human readable identifier (e.g. XXX-123)
  </ResponseField>
  <ResponseField
    name="estimate"
    type="number"
  >
    The estimate of the issue in points
  </ResponseField>
  <ResponseField
    name="url"
    type="string"
    required
  >
    The URL of the issue on Linear
  </ResponseField>
    </Expandable>
  </ResponseField>
</Expandable>


{/* vale on */}