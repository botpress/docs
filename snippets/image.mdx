export const Image = ({ alt, src, srcDark }) => (
  <Frame>
    <img
      alt={alt}
      className="block dark:hidden"
      src={src}
    />
    <img
      alt={alt}
      className="hidden dark:block"
      src={srcDark ? srcDark : src}
    />
  </Frame>
);