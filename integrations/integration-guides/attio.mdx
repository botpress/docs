---
description: Add a bot to Attio using the official integration.
---

{/* vale off */}

import integrationVersions from '/snippets/integrations/versions.mdx'
import { OpenInHub } from '/snippets/integrations/open-in-hub.jsx'
import Cards from '/snippets/integrations/cards/botpress/attio.mdx'
import Triggers from '/snippets/integrations/triggers/botpress/attio.mdx'

<OpenInHub integration={integrationVersions.attio}/>

{/* vale on */}

The official Attio integration allows your bot to interact with objects/records in your Attio workspace.

## Setup

<Info>
  You will need:

  - A [published bot](/learn/get-started/quick-start)
  - A [Attio account](https://app.attio.com/welcome/sign-in)
</Info>

<Steps>
  <Step title="Install the integration in Botpress">
    First, install the integration to your bot:

    1. In Botpress Studio, select **Explore Hub** in the upper-right corner.
    2. Search for the **Attio** integration, then select **Install Integration**.

    In the **Configuration** menu, you should see one field: **Access Token**. Leave it empty for now—you'll come back to it after creating your Attio API token.
  </Step>
  <Step title="Create an Attio API token">
    Next, create a Attio API token:

    1. [Log in to your Attio account](https://app.attio.com/welcome/sign-in).
    2. Select your workspace name in the upper-left corner. Then, select **Workspace settings**.
    3. From the left navigation bar, select **Developers**.
    4. Select **+ Create access token**. Enter a name, then enable the following scopes:
        - **Object configuration**: Read access
        - **Records**: Read/write access
    5. Select **Save changes**, then copy the token.
  </Step>
  <Step title="Configure the integration in Botpress">
    Now, you can finish setting up the integration in Botpress:

    1. In the integration's **Access Token** field, paste your Attio API token.
    2. Select **Save Configuration**.
  </Step>
</Steps>

<Check>
  Your Attio integration is ready. You can use the integration's [Cards](#cards) to interact with your Attio objects and records..
</Check>

## Cards

<Note>

  When adding [filters](#param-filter) to the [List Records](#list-records) Card, follow [Attio's filtering guide](https://docs.attio.com/rest-api/how-to/filtering-and-sorting) to structure your data.

</Note>

<Cards />

## Triggers

<Triggers />
