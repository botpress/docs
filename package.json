{
  "name": "botpress_docs",
  "version": "12.26.3",
  "description": "The world's most powerful conversational engine",
  "scripts": {
    "postinstall": "cd website && yarn",
    "start": "cd website && yarn && yarn start",
    "build": "cd website && yarn && yarn build",
    "version-docs": "node version-docs"
  },
  "dependencies": {
    "prettier": "^2.4.0",
    "rimraf": "^3.0.2"
  }
}
